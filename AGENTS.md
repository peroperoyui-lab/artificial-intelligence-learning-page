# AI 编码助手工作入口

先阅读 [CONTRIBUTING.md](CONTRIBUTING.md)；定位文件及扩展接点见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)。需要修改视觉或教学结构时，再读 [docs/DESIGN.md](docs/DESIGN.md)。这些文档与人类贡献者共用，不另设一套架构。

## 开工

- 先检查当前分支、工作区、相关代码和测试，不覆盖未提交工作或他人提交。
- 围绕本次任务修改；无需为了符合抽象的“最佳实践”重写框架或格式化整个仓库。
- 默认维护现有离线能力和零生产依赖；新的框架、模型或在线扩展按贡献指南说明必要性和兼容影响。

## 实现

- 复用 `AI.E`、`AI.C`、`AI.U`、`AI.labs`，遵守现有脚本加载与实验清理约定。
- 数值、图形、说明和测试同步；新增课程同步检查路由、固定数量、词典、来源及学习记录。
- 保留用户数据和模型协议兼容；导入文件只作为数据处理，用户文本须安全转义。
- 修改源码而非 `dist/`；不提交凭据、个人数据或临时产物。

## 验证与交付

按贡献指南选择实际必要的验证：`npm test`、`npm run build`、`npm run test:e2e`。默认浏览器模式与 `AI_TEST_IN_MEMORY=1` 的覆盖范围不同，必须明确报告通过、失败和跳过项。

提交前检查 diff、文档链接与许可。中文说明修改内容、验证结果和剩余限制；不要把未运行的检查、未完成的部署或推测的效果写成已验证事实。
