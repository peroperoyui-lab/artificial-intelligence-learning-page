/* Additional explanations and small case studies; original prose, MIT. */
(function (root) {
  'use strict';
  const C = root.AI.C;
  C.sources.push(...[
    ['channels', '动手学深度学习 · 多输入多输出通道', 'https://zh.d2l.ai/chapter_convolutional-neural-networks/channels.html', '通道求和、输出通道与 1×1 卷积'],
    ['pooling', '动手学深度学习 · 汇聚层', 'https://zh.d2l.ai/chapter_convolutional-neural-networks/pooling.html', '最大/平均池化及空间尺寸'],
    ['padding', 'Dive into Deep Learning · Padding and Stride', 'https://d2l.ai/chapter_convolutional-neural-networks/padding-and-strides.html', '填充、步幅与输出形状'],
    ['transformer-block', '动手学深度学习 · Transformer', 'https://zh.d2l.ai/chapter_attention-mechanisms/transformer.html', '残差、层归一化、逐位置前馈网络与编码器/解码器'],
    ['position', 'Dive into Deep Learning · Self-Attention and Positional Encoding', 'https://d2l.ai/chapter_attention-mechanisms-and-transformers/self-attention-and-positional-encoding.html', '自注意力、正弦位置编码及计算复杂度'],
    ['clustering', 'scikit-learn · Clustering', 'https://scikit-learn.org/stable/modules/clustering.html#k-means', 'Lloyd 算法、簇内平方和、K-means++ 与局限'],
    ['preprocessing', 'scikit-learn · Preprocessing data', 'https://scikit-learn.org/stable/modules/preprocessing.html', '标准化、缩放及预处理'],
    ['dropout', '动手学深度学习 · 暂退法', 'https://zh.d2l.ai/chapter_multilayer-perceptrons/dropout.html', 'Dropout 的训练/推断区别'],
    ['validation', 'scikit-learn · Common pitfalls', 'https://scikit-learn.org/stable/common_pitfalls.html', '数据泄漏及训练/测试分离']
  ].map(([id, title, url, use]) => ({ id, title, url, use })));

  // Keep every original chapter ID and saved-note key unchanged.
  const additions = {
    map: [
      ['先分清任务、方法与训练信号', '“识别垃圾邮件”是任务，“神经网络”是模型方法，“用已标注邮件训练”是监督学习。三者处在不同分类轴上。一个任务可以用规则、逻辑回归或神经网络实现；同一个网络结构也可以服务于分类、回归或生成。先说清输入、输出和可获得的监督信号，才知道该选什么方法。'],
      ['训练和推断分别发生了什么', '训练时把样本送入模型，计算损失和梯度并更新参数。推断时参数固定，只根据新输入计算输出。调学习率改变训练过程；调生成温度改变采样分布。页面中有真实训练的 MLP，也有固定权重的 Transformer 前向实验，它们展示的是不同环节。'],
      ['从任务需求建立一个可检验的流程', '例如把一篇文章分为体育、教育、经济：先定义标签规则和标注样本，再划分训练、验证、测试集，建立简单基线，最后检查新文章上的错例。文本转数字的方法、分类器和评价指标是三个可替换部件。做出一个会输出标签的程序只是开始，还需要说明它在哪些数据上有效。']
    ],
    vectors: [
      ['把 shape 当成每一步的尺寸标签', '单张灰度图可写为 8×8；加入通道轴是 1×8×8；一次送入 16 张则是 16×1×8×8。这里第一维是批量 N，中间是通道 C，最后是高 H 和宽 W。另一些库使用 N×H×W×C，所以“维度都是四个”不保证数据顺序相同。张量的轴数和某条轴的长度需要分开说。'],
      ['点积、矩阵乘法和逐元素乘法', '向量 [1,2] 与 [3,4] 的点积是 1×3+2×4=11；逐元素相乘得到 [3,8]。矩阵 A 为 2×3、B 为 3×4 时，AB 为 2×4，每个输出格都是一行与一列的点积。神经网络中的 XW 使用矩阵乘法；门控和部分激活导数会用逐元素乘法。'],
      ['为什么输入尺度会影响学习', '把身高从“米”改成“厘米”，相同含义的数字会扩大 100 倍。这会改变点积、距离和梯度的尺度。标准化常用 (x−训练均值)/训练标准差；验证和测试集沿用训练统计量。它不是增加信息，而是让模型更容易在可比较的数值尺度上计算。']
    ],
    regression: [
      ['一个样本，手算一次预测与更新', '设 x=2、y=5、w=1、b=0，则预测为 2，残差为 −3，单样本平方损失为 9。梯度 ∂L/∂w=2(2−5)×2=−12，∂L/∂b=−6。学习率为 0.1 时，两者同时用旧参数的梯度更新为 w=2.2、b=0.6，新预测是 5，新损失为 0。这只是这个单样本的特例。'],
      ['参数数量怎样计算', 'd 个输入、一项输出的线性模型有 d 个权重和 1 个偏置，即 d+1 个参数；m 个输出时为 d×m+m。样本变多增加了训练计算，但不会自动增加这个模型的参数数目。多层网络逐层累加这个公式，因此输入节点本身不算可训练参数。'],
      ['逻辑回归为什么也叫回归', '逻辑回归先计算线性分数 z=w·x+b，再用 Sigmoid 把 z 映射为二分类概率，并用交叉熵训练。它常用于分类任务，与直接输出连续数值、使用平方损失的线性回归要区分。本项目训练场把隐藏层设为 0 时，就是一个二输入的逻辑回归模型。']
    ],
    neuron: [
      ['一个神经元保留了哪些数值', '输入 x 是样本特征，权重 w 和偏置 b 是参数，z=w·x+b 是激活前值，a=φ(z) 是激活值。它们不是四种不同的模型，而是一次前向运算中先后出现的变量。一个层把多个神经元并排计算，形成一组输出特征。'],
      ['线性层为什么不能无限堆出新能力', '若两层都不含非线性，W₂(W₁x+b₁)+b₂=(W₂W₁)x+(W₂b₁+b₂)，仍然只是一个仿射变换。隐藏层不是单凭“多了一层”就能弯曲分类边界。tanh、ReLU 等非线性让分段组合成为可能，但表达能力增加后仍要靠数据和优化学出有效参数。'],
      ['导数决定局部信号能传多强', 'Sigmoid 在 z=0 附近导数最大，为 0.25；当 |z| 很大时导数接近 0，局部变化传得很弱。ReLU 在正半轴导数为 1，负半轴为 0；一个神经元若长期对所有样本都输出负的激活前值，可能收不到有效更新。初始化、输入尺度与学习率都值得一起检查。']
    ],
    loss: [
      ['从 logit 到概率，再到交叉熵', '二分类分数 z 是 logit，不限制在 0–1。p=Sigmoid(z)，相应 log-odds 为 log(p/(1−p))=z。真实标签 y=1 时，p=0.9 的损失约为 0.105，p=0.1 时约为 2.303。多类时把所有类别 logits 一起送入 Softmax，再取真实类别概率的负对数。'],
      ['损失、指标和目标函数', '训练通过梯度最小化一个平滑损失；准确率只看分类是否正确，通常不能直接提供有用的梯度。加入 L2 后，优化目标可以是“平均数据损失+正则项”。训练场曲线显示的是不含 L2 的平均 BCE；所以阅读任何损失图前，先确认它究竟画了哪一项。'],
      ['数值稳定性不是更换数学目标', '直接计算 exp(1000) 会溢出。Softmax 可以先从所有 logits 减去它们的最大值，概率不变。二分类 BCE 也能直接根据 logit 稳定计算，避免先把概率舍入为 0 或 1 再取 log。显示为 100.0% 的概率可能只是显示舍入，底层浮点数和真实模型的不确定性仍需区分。']
    ],
    descent: [
      ['Epoch、Batch 和 Step 到底谁在计数', '训练场有 180 条训练样本，batch=16 时，一轮需要 ceil(180/16)=12 次参数更新；最后一批只有 4 条样本。100 个 epoch 对应 1200 次更新，不是 100 次。全批量梯度下降每轮只更新一次；“随机梯度下降”在实践中也常泛指小批量训练。'],
      ['动量与 Adam 记住了什么', '动量积累带方向的梯度，使连续相同方向的更新得到延续。Adam 同时维护梯度的一阶矩和平方梯度的二阶矩，并进行初始偏差校正，再按分量缩放更新。二阶矩是平方的滑动平均，不是 Hessian，也不是直接求二阶导。不同优化器中的同一个学习率通常不可简单等效比较。'],
      ['学习率调大并不等于训练更快', '对 L(w)=w²，梯度下降给出 w下一步=(1−2η)w。0<η<1 时误差幅度缩小；η=1 时可在两侧来回跳；η>1 时幅度放大。真实损失曲面比这复杂，实验中的发散是理解步长与曲率关系的入口。学习率调度可随训练降低步长，不能替代合理的数据与目标。']
    ],
    backprop: [
      ['反向传播与优化器分工', '反向传播求的是每个参数对损失的局部影响，即梯度；优化器再决定怎样利用梯度修改参数。固定同一前向图与损失时，SGD 和 Adam 可以接收同一组梯度，却走出不同更新轨迹。把“反向算梯度”和“更新权重”分成两步，更容易定位错误。'],
      ['一条参数影响多个输出时，要相加', '若同一个权重参与了两个计算分支，它对总损失的梯度是两条路径贡献之和。卷积核在不同位置共享，同一参数从多个窗口获得梯度；批量损失取平均时，又要按样本数缩放。漏掉求和、额外除了一次批量大小，都会让看似合理的公式变成错误更新。'],
      ['自动微分、数值差分和符号求导', '自动微分按计算图和各运算的导数规则执行链式法则，反向模式适合“很多参数、一个标量损失”。中心差分用两次前向计算近似一个偏导，适合检查少量参数，不适合替代大网络训练。它也受差分步长和浮点误差影响，ReLU 的折点需要单独约定。']
    ],
    playground: [
      ['先用一个有意义的基线', '默认 XOR 数据需要弯曲的决策边界。先用 0 隐藏层训练，再比较两层 tanh 网络；只改变一项配置并记录结果，才能知道变化来自哪里。换成线性可分数据后，简单模型已经可能足够。较复杂的网络训练损失更低，不自动意味着它更适合新数据。'],
      ['背景、连接和节点分别表示什么', '概率场的每个小格都把该位置的坐标输入当前网络，颜色是预测的类别 1 概率。连接的颜色和粗细表示权重符号与幅度，节点表示一次指定输入下的激活值。因此“连接很粗”和“这个特征很重要”之间还隔着输入、非线性与其他路径，不能只凭一条边判断模型机制。'],
      ['复现一次实验需要保存哪些东西', '除数据种子、结构和权重外，接着训练还需要优化器状态、批次随机状态和已完成轮次。本项目模型文件把这些一起保存；重新导入后重新生成相同数据。改配置会重置实验，避免把不同训练条件画成同一条连续曲线。实验本保存的是观察快照，模型 JSON 才能用于继续训练。']
    ],
    generalization: [
      ['先把三个数据角色区分开', '训练集参与参数更新；验证集用来选复杂度、阈值和停止轮次；测试集用于方案确定后的最终评价。若你反复看同一个测试集并据此修改模型，它实际上已经参与选择。这个教学实验只设置训练与验证，不提供可以宣称最终泛化能力的独立测试。'],
      ['早停、Dropout 与 L2 解决问题的角度不同', '早停依据验证表现选择训练何时结束；L2 对较大的权重施加代价；Dropout 在训练时随机屏蔽部分激活并进行尺度补偿，推断时关闭这种随机屏蔽。它们都有适用条件，不能保证复杂模型一定优于简单模型。本页实际演示多项式复杂度和 L2，其他方法由文字解释。'],
      ['数据泄漏常出现在模型以外', '例如用全体样本的均值补缺、标准化或选择特征，再划分测试集，测试分布的信息可能已进入训练。文本重复、同一人的多次记录、时间穿越也会让分数虚高。预处理参数应在训练集拟合；时间预测应尊重先后顺序，相关样本则考虑分组划分。']
    ],
    metrics: [
      ['一个不平衡任务的手算例子', '100 条样本只有 10 条正类，把所有样本预测成负类也有 90% 准确率，却漏掉全部正类。若模型报出 8 条正类，其中 6 条正确，则 TP=6、FP=2、FN=4，精确率=6/8=75%，召回率=6/10=60%，F1=12/18≈66.7%。'],
      ['阈值是在模型训练之后做的决策', '同一组预测概率可以配不同阈值。降低阈值一般会报出更多正类，提高召回，同时也可能增加误报。究竟优先哪种错误，取决于业务代价。阈值应在验证数据上选择；精确率的分母为空时是未定义，而不是把它武断记为 100%。'],
      ['概率大不一定代表概率校准得好', '如果模型给出 0.8 的一组预测长期只有约 0.5 成真，它的概率就偏自信。校准关注预测概率与经验频率是否一致；排序指标关注谁排在前面；准确率关注一个阈值下的决定。真实评价还应检查类别、群体和分布变化，单个汇总分数往往掩盖错例。']
    ],
    convolution: [
      ['先理解核为什么能找边缘', '核的一边是负数、另一边是正数时，计算的是局部两侧亮度差。均匀区域的正负贡献抵消，边界处则产生明显响应。Sobel x 对水平方向的亮度变化敏感，常突出竖向边缘；负响应表示相反方向的变化，不等于计算出错。边界补 0 本身也可能产生响应。'],
      ['一个输出通道怎样汇合多个输入通道', 'RGB 图像有 3 个输入通道。一个输出通道需要 3 片空间核，分别与 R/G/B 做互相关，把结果相加并加一个偏置。若有 16 个输出通道、3×3 核，普通卷积含 16×3×3×3+16=448 个参数；并不是给每个像素单独配 448 个参数。输出通道数由所需特征数量决定。'],
      ['步幅、填充和感受野', '无膨胀时，输出边长为 floor((H+2p−k)/s)+1。H=8、k=3、p=1、s=1 时仍输出 8；改 s=2 得到 4。感受野是一个输出能受到哪些原始像素影响：连续两层 3×3、步幅 1 的卷积，有效感受野变为 5×5。具体大小还受池化、步幅、膨胀等影响。'],
      ['从滤镜走向一个小视觉系统', '第 14 章把 8×8 图案依次变成 3 个卷积响应、ReLU 输出、4×4 池化特征，再展平为 48 个数，训练一个三分类头。三片卷积核保持固定，所以它展示固定特征提取加可训练分类器；完整 CNN 还会让卷积权重通过反向传播一起学习。']
    ],
    attention: [
      ['Q、K、V 是从哪里来的', '标准自注意力把同一输入矩阵 X 投影为 Q=XWq、K=XWk、V=XWv，投影矩阵通常是模型训练的参数。Q 与 K 的点积决定匹配分数，经过 Softmax 后的权重再汇总 V。Q/K/V 不是三份不同的文本，也不要求每个分量都有可以命名的语义。原实验把它们直接设为教学数字，下一专题补上投影过程。'],
      ['为什么除以 √dₖ，Softmax 又沿哪条轴', '当向量分量尺度相近时，维度增加可能使点积的波动增大。除以键/查询维度的平方根可缓和这个尺度变化。Softmax 对一个查询对应的所有键分数逐行归一化，因此每一行权重之和为 1；不同列之和一般不是 1，注意力也不是一张双随机矩阵。'],
      ['多头、自注意力和交叉注意力', '多个头使用不同的投影子空间，分别匹配和汇总，再拼接并经过输出投影。自注意力的 Q/K/V 来自同一序列；交叉注意力的 Q 来自一个序列、K/V 来自另一个序列，例如解码器查询编码器输出。不同头不保证自动对应某个固定的语法规则，未经训练的头更没有这种保证。'],
      ['Transformer 除了注意力还有什么', '一个常见块还含残差连接、归一化与逐位置前馈网络。原论文采用 Post-LN 结构，很多变体调整为 Pre-LN；位置编码也有正弦、可学习和旋转位置编码等方案。本项目第 15 章使用四维、至多 8 个位置的 Post-LN 数值实例，把这条流程完整算一遍。']
    ],
    tokens: [
      ['词元编号和嵌入向量之间', '分词器先把文本切成离散词元，再查词表得到整数编号。嵌入层用编号从 V×d 参数表中取出 d 维向量；编号大小本身没有距离含义。实际分词可能切成汉字、子词或字节片段。为了便于观察，Transformer 专题明确使用八项人工词表，不模拟真实 BPE 分词器。'],
      ['训练下一个词元和实际生成', '给定序列 [A,B,C,D]，因果语言建模可同时训练 A→B、AB→C、ABC→D，未来位置通过掩码屏蔽。生成时先预测下一个词元，把选出的词元接回上下文，再重复前向计算。当前采样页从同一人工分布反复抽样，不会更新上下文；Transformer 专题同样只做单次前向数值追踪。'],
      ['温度、top-k、top-p 的先后和作用', '温度用 logits/T 调整集中程度，top-k 只保留排名前 k 项，top-p 则保留累计概率达到阈值的最小前缀，再重新归一化。不同实现会约定不同处理顺序，阅读代码时要确认。本页实现温度与 top-k；top-p 在词典解释。T 趋近 0 的极限趋向最高分项，程序中应单独实现贪心而不是直接除以 0。'],
      ['困惑度与“说得对不对”', '使用自然对数的平均词元负对数似然为 L 时，困惑度为 exp(L)。它衡量模型给真实序列分配概率的情况，依赖测试语料和分词口径。它不是事实正确率，也不能把采用不同词表、不同任务的数据分数直接横向排名。']
    ]
  };
  for (const [id, paragraphs] of Object.entries(additions)) C.chapters.find(c => c.id === id).body.push(...paragraphs.map(([h, p]) => ({ h, p })));
  const addRefs = (id, refs) => C.chapters.find(c => c.id === id).refs.push(...refs);
  addRefs('vectors', ['preprocessing']); addRefs('generalization', ['dropout', 'validation']);
  addRefs('convolution', ['channels', 'pooling', 'padding']); addRefs('attention', ['transformer-block', 'position']);
  C.groups.push({ title: '轻量专题', subtitle: '把多个部件连接成可检查的小系统', range: '14—16' });
  C.chapters.push(
    { id: 'cnn', short: '小图像分类流水线', title: '从像素，到一个可训练的分类头。', group: 3,
      lead: '8×8 像素，三片固定卷积核，147 个可训练参数。看清每一个中间结果，再让分类头学习三种线条。',
      summary: '特征提取把图像变为数字，分类头用这些数字学习类别。',
      challenge: '先训练 40 轮，再移动竖线、加噪声或画一张空白图。比较中间特征与预测概率，尤其留意“没见过的图”也会得到概率。',
      prereqs: ['convolution', 'loss', 'generalization'], refs: ['conv', 'channels', 'pooling', 'ce', 'validation'],
      body: [
        { h: '先把一张图拆成三种局部响应', p: '同一张灰度图依次经过 Sobel x、Sobel y 和 Laplacian 三片人工核，得到三个输出通道。它们分别强调横向亮度差、纵向亮度差和邻域亮度差。输入为 1×8×8，步幅 1、零填充 1，输出为 3×8×8。每个输出格都是 9 项乘积之和，窗口边缘以 0 补齐。' },
        { h: 'ReLU 与池化分别丢掉什么', p: 'ReLU 把负响应归零，保留正响应；它不改变形状。2×2、步幅 2 的池化把每四个邻近值压成一个数，使每个通道由 8×8 变为 4×4。最大池化保留最强响应，平均池化保留局部平均强度。它们都丢掉窗口内部的一部分细节；池化本身没有训练权重。' },
        { h: '48 维特征为什么对应 147 个参数', p: '三个通道各有 4×4=16 个数，按通道、行、列顺序展平得到 48 维向量。三分类线性头含 48×3=144 个权重和 3 个偏置，总计 147 个可训练参数。每个类别输出一个 logit，Softmax 形成三项概率。这里是多类 Softmax，不是三个独立 Sigmoid。' },
        { h: '这里究竟训练了哪部分', p: '合成 120 张线条图，三类各 40 张；按类划为 90 条训练、30 条验证。特征在初始化时预计算。训练用全批量梯度下降更新分类头，卷积核保持冻结，没有反向更新。因此“第 1 轮”只有一次参数更新；最大 200 轮。切换池化方式会重置分类头和曲线。' },
        { h: '预设图案能用来检查什么', p: '竖线、横线和斜线提供三个已定义类别，移动位置与加入噪声可以检查模型是否只记住某个固定像素。你也可以点击像素或输入行列来修改图案。空白、十字或复杂涂鸦属于训练分布之外，分类器仍会被迫给三类分配总和为 1 的概率；这些数字不构成对未知图案的可靠识别。' },
        { h: '与完整 CNN 的距离', p: '完整 CNN 会在数据上共同学习卷积核与分类头，可能含更多通道、卷积块和归一化。本例选固定滤镜是为了把每一层作用拆开验证，不声称复现手写数字识别或预训练视觉模型。所有素材程序生成，没有图片上传和模型下载。' }
      ],
      deep: { title: '展开：多通道尺寸、池化与分类头梯度', html: '<p>卷积按互相关约定：Z[c,y,x]=ΣᵢΣⱼ K[c,i,j]X[y+i−1,x+j−1]。三片核分别除以 4，显示与分类使用相同数值。</p><div class="formula">1×8×8 → 3×8×8 → ReLU → 3×4×4 → 48 → 3</div><p>分类头 zⱼ=ΣᵢWⱼᵢfᵢ+bⱼ；单样本梯度 ∂L/∂Wⱼᵢ=(pⱼ−1[y=j])fᵢ，∂L/∂bⱼ=pⱼ−1[y=j]。90 条训练样本的梯度取平均后统一更新，不含 L2。验证样本只用于评估。</p><p>8×8 的三通道卷积共执行 8×8×3×9=1728 次核乘加。训练时复用预计算的 48 维特征；显示刷新仅计算当前小图，计算规模有明确上限。</p>' },
      quiz: { q: '这个实验训练 40 轮后，哪一部分被梯度更新？', options: ['三片 Sobel/Laplacian 卷积核', '48→3 分类头的权重与偏置', '最大池化窗口中的全部像素'], answer: 1, why: '卷积核固定，输入图案不是参数；只有 147 个分类头参数在学习。' }
    },
    { id: 'transformer', short: '拆开 Transformer 块', title: '一小段序列，走完一个 Transformer 块。', group: 3,
      lead: '最多 8 个词元、4 维表示、1 或 2 个注意力头。单步查看投影、残差、归一化与前馈网络，所有矩阵都实时计算。',
      summary: '注意力混合位置之间的信息，前馈网络变换每个位置的特征。',
      challenge: '关掉位置编码和因果掩码，交换前两个词元，比较随词元一起移动的输出行；再打开位置编码，观察这种对应关系怎样改变。',
      prereqs: ['vectors', 'neuron', 'attention', 'tokens'], refs: ['transformer', 'transformer-block', 'position', 'ce'],
      body: [
        { h: '从离散编号到连续表示', p: '实验词表只有八项。每个词元按编号查 8×4 的嵌入表，乘以 √4=2 后加正弦位置编码，形成 T×4 输入。相同词元共享同一个嵌入，但位于不同位置时可有不同位置向量。所有权重由固定种子初始化，没有语言训练，词语只是方便观察的位置标签。' },
        { h: '同一个 X 经历三种投影', p: 'Q、K、V 都由 T×4 的 X 乘以各自的 4×4 权重矩阵得到。双头模式下，每个投影按最后一维拆成两组，每组 T×2；分别计算缩放点积、掩码、Softmax 和对 V 的加权汇总。保持总表示宽度为 4，因此切换头数不改变投影参数总量。' },
        { h: '拼接之后还要做输出投影', p: '两个头的 T×2 结果按特征轴拼成 T×4，再乘 4×4 的 Wo。不能把两个头的概率矩阵直接相加当作最终输出。界面既能切换查看某个头，也能检查拼接后的矩阵以及最终投影，让“多头”不只是两张不同颜色的图。' },
        { h: '残差与 LayerNorm 如何协作', p: '本例使用 Post-LN：H=LayerNorm(X+Attention(X))。残差让输入沿加法通路保留；LayerNorm 对每个词元行内的四个特征算均值与总体方差，再缩放。它不跨词元求均值，也不同于按批次统计的 BatchNorm。可以分别关闭残差或归一化做消融，关闭后展示的是实验变体。' },
        { h: '前馈网络不在这里混合不同位置', p: '同一组 4→8→4 权重分别作用于每一个位置，中间用 ReLU。随后再次残差相加和 LayerNorm。注意力负责不同位置之间的交互，逐位置前馈负责特征变换，两者的工作不同。最后演示一个 4→8 词表投影；输出概率来自未训练权重，不能据此判断句子语义。' },
        { h: '因果、非因果与完整语言模型', p: '打开因果掩码后，位置 i 不读取后面位置；前馈和 LayerNorm 也只处理同一行，因此改末尾词元不会改变更早位置的输出。关闭掩码是非因果自注意力。原始编码器—解码器 Transformer 还含交叉注意力、更多层与任务训练；本实验只做一个单块的前向追踪，不训练、不自动生成文本。' },
        { h: '性能预算和简化约定', p: '最多只有 8×8 的单头注意力矩阵，使用普通 JavaScript 和 CPU，不加载权重文件、不依赖 WebGPU。省略 Dropout，注意力投影不加偏置；前馈和输出头偏置固定为 0；LayerNorm 的 γ=1、β=0。共展示 228 个参数位置，当前全部冻结。统计量随当前设备不同，不能把开发环境用时当作手机保证。' }
      ],
      deep: { title: '展开：形状、Post-LN 公式与参数账本', html: '<div class="formula">Q=XWq, K=XWk, V=XWv<br>Aₕ=softmax(QₕKₕᵀ/√dₖ + mask)<br>M=Concat(A₁V₁,…,AₕVₕ)Wo<br>H=LN(X+M)<br>Y=LN(H+ReLU(HW₁+b₁)W₂+b₂)</div><p>LayerNorm(x)=(x−μ)/√(v+10⁻⁵)，这里 γ=1、β=0，v 用分母 d=4 的总体方差。因此加了 epsilon 后，输出方差通常略小于 1；常量行输出全 0。</p><p>参数位置：嵌入 8×4=32；Q/K/V 投影 3×4×4=48；输出投影 4×4=16；前馈 4×8+8+8×4+4=76；两组 LayerNorm 2×(4+4)=16；词表头 4×8+8=40。合计 228。1 头与 2 头使用相同的总宽度，参数数目相同。</p><p>PE(pos,2i)=sin(pos/10000^(2i/d))，PE(pos,2i+1)=cos(pos/10000^(2i/d))。这是可计算的正弦位置编码，不是词元语义。去掉位置编码与因果掩码时，交换输入行会相应交换输出行（置换等变）。</p><p>朴素注意力计算需要 T×T 分数与权重，位置数翻倍会让这些格子增为四倍。本例设置硬上限 8，避免用大型序列制造卡顿。</p>' },
      quiz: { q: '关闭位置编码和因果掩码后，交换两个输入词元，理论上会怎样？', options: ['参数自动重新训练', '输出行按相同方式交换，其他行保持对应', '每行概率都必然变为均匀分布'], answer: 1, why: '这一配置对输入位置置换等变。加入位置或因果结构后，这个对应关系一般不再成立。' }
    },
    { id: 'clustering', short: 'K-means 聚类', title: '没有答案标签，也能寻找分组。', group: 3,
      lead: '把“分配到最近中心”和“移动中心到均值”拆成两个按钮动作。比较初始化、簇数量与数据形状，而不把聚类编号误当真相。',
      summary: 'K-means 交替分配和更新，降低簇内平方距离。',
      challenge: '先在三团点上跑 K=3，再换成同心圆。观察目标函数确实下降，却未必得到你直觉中的内圈与外圈。',
      prereqs: ['map', 'vectors'], refs: ['clustering', 'preprocessing'],
      body: [
        { h: '两步就能实现的循环', p: '先为每个样本找到最近的中心，用中心编号作为当前簇；再把每个中心移动到所属样本的坐标均值。重复这两步，直到中心几乎不动或达到轮次上限。算法只使用二维坐标；即使合成数据由几种图案生成，真实类别也不参与分配和更新。' },
        { h: '目标函数是什么', p: '簇内平方和 J=Σᵢ||xᵢ−μ所属簇||²。固定中心时，选最近中心不会增加 J；固定分配时，均值最小化该组平方误差。每个半步都可直接验证这个性质。下降只表示这个数学目标变好，并不保证找到了有业务意义的分类。' },
        { h: '为什么需要初始化和多个尝试', p: '目标一般有局部最优，随机初始中心可能导致不同结果。K-means++ 先选一个样本，再按到已有中心的最近平方距离抽取新中心，倾向于把起点分开。它提高起点质量的机会，但不保证一次求全局最优。本例保留随机样本初始化供对照，种子只改变初始化。' },
        { h: '簇数量、尺度和形状都是假设', p: 'K 是你事先给定的超参数。增加 K 往往让平方和下降，所以不能单凭最小平方和选择簇数。欧氏距离还依赖特征尺度；拉长某一轴会改变“近”的意义。同心圆实验表明，K-means 的近中心分配更擅长紧凑簇，不适合把所有弯曲形状都还原出来。' },
        { h: '空簇、停止与解释', p: '若某个中心没有样本，本例让它保持原位，并在簇计数中显示 0；不把空集均值算成 NaN。最多 40 轮，离页或切后台暂停。中心是均值，可以不落在任何真实样本上；簇 1、簇 2 的编号没有排序或语义含义。' }
      ],
      deep: { title: '展开：均值更新、距离与 K-means++', html: '<div class="formula">aᵢ=argminⱼ ||xᵢ−μⱼ||²<br>μⱼ=Σᵢ:aᵢ=j xᵢ / nⱼ<br>J=Σᵢ ||xᵢ−μₐᵢ||²</div><p>对某簇的中心 μ 求导，∂Σ||x−μ||²/∂μ=2nμ−2Σx，令其为 0 得到均值。n=0 时采用保留旧中心的明确约定。</p><p>K-means++ 在已有中心集合上计算 D(x)²=minⱼ||x−μⱼ||²，新中心的采样概率与 D(x)² 成正比。若所有距离为 0，选择尚未用过的样本索引，避免除以 0。距离相等时，分配给编号最小的中心。</p><p>这一小实验每步至多 120 点×5 中心×2 坐标，不需要神经网络、反向传播或显卡。肘部法、轮廓系数和稳定性可辅助选择 K，最终仍要结合研究问题解释。</p>' },
      quiz: { q: 'K 从 2 增加到 5 后平方和更低，可以直接断言真实世界有五类吗？', options: ['可以，平方和就是正确类别数', '不可以，更多中心本来就能减小距离，还需结构与任务依据', '可以，只要换成 K-means++'], answer: 1, why: '优化目标与真实类别含义是两件需要连接的事；K 更大时平方和更小不足以完成这个论证。' }
    }
  );
  C.chapters.forEach((c, i) => { c.no = String(i + 1).padStart(2, '0'); });

  const rows = `
监督学习|Supervised learning|用输入与目标输出成对的样本学习映射。|map|邮件文本配上垃圾/正常标签；标签提供训练信号。
无监督学习|Unsupervised learning|没有预先给定的任务标签，从数据结构中学习表示或分组。|clustering|K-means 只看坐标，不读取图案生成时的类别。
强化学习|Reinforcement learning / RL|智能体通过与环境交互，依据回报学习行动策略。|map|棋类智能体依据后续胜负更新策略；本站只做概念定位。
分类|Classification|预测一个样本属于哪一类别，或给各类别概率。|metrics|把文章分为体育、教育、经济，输出是离散类别。
回归|Regression|预测连续数值的任务。|regression|根据面积预测房价；输出不是类别编号。
模型|Model|规定输入如何变成输出、以及哪些量可以学习的函数或系统。|map|线性模型写作 ŷ=wx+b，训练决定 w 和 b。
参数|Parameter|模型内部通过训练学习、推断时通常固定的数值。|playground|权重、偏置和嵌入表可成为参数；当前输入不是参数。
超参数|Hyperparameter|在一次训练或结构设计中预先设定的控制量。|playground|学习率、隐藏层宽度与 batch size 都是超参数。
训练|Training|利用样本和学习目标估计或更新模型参数。|map|点击训练场开始，权重会根据梯度变化。
推断|Inference|在固定参数下为输入计算模型输出。|map|已训练模型预测一个新坐标；这一步不更新权重。
生成模型|Generative model|建模数据分布，并可据此生成样本的一类模型。|tokens|自回归语言模型依次预测词元；生成不保证事实正确。
样本|Sample / Example|数据集中一次完整的输入记录，监督任务常含标签。|vectors|一个二维点加 y 标签是一个样本，而不是两个样本。
数据集|Dataset|按任务组织的一组样本。|generalization|240 个点组成训练场数据集，再分训练与验证。
形状|Shape|按轴顺序写出的张量各维长度。|vectors|16×3×8×8 可表示 16 张三通道小图。
点积|Dot product|对应分量相乘再求和，结果为一个标量。|vectors|[1,2]·[3,4]=11；注意力用它计算匹配分数。
矩阵乘法|Matrix multiplication / Matmul|用左矩阵的行与右矩阵的列做点积形成新矩阵。|vectors|2×3 乘 3×4 得到 2×4。
逐元素乘法|Elementwise multiplication / Hadamard product|相同位置的分量直接相乘。|vectors|[1,2] 与 [3,4] 逐元素相乘是 [3,8]。
转置|Transpose|交换矩阵的行轴与列轴。|vectors|3×4 矩阵转置为 4×3，元素对应关系一起交换。
范数|Norm|度量向量大小的一类函数。|vectors|[3,4] 的 L2 范数是 √(9+16)=5。
广播|Broadcasting|按规定扩展长度为 1 的维度，使不同形状参与逐元素运算。|vectors|T×4 的矩阵每行加同一个 4 维偏置，不必实际复制参数。
独热编码|One-hot encoding|用一项为 1、其余为 0 的向量表示离散编号。|vectors|三类中的第 2 类可写为 [0,1,0]；编号从哪开始要约定。
标准化|Standardization / Z-score|常指减去训练均值并除以训练标准差的预处理。|vectors|把厘米和千克特征拉到较可比的数值尺度；测试集沿用训练统计量。
线性回归|Linear regression|用输入的线性组合加偏置预测连续目标的模型。|regression|两项特征时为 ŷ=w₁x₁+w₂x₂+b。
逻辑回归|Logistic regression|通过线性分数和 Logistic 链接建模二分类概率。|regression|训练场设 0 隐藏层时，用 Sigmoid(wx+b) 预测概率。
残差|Residual error|回归中预测值与真实值的差；需声明相减顺序。|regression|本教程用 ŷ−y；不要与网络的残差连接混为一谈。
目标函数|Objective function|优化算法实际最小化或最大化的量。|loss|平均 BCE 加 L2 可作为目标，曲线也可以只画 BCE。
神经元|Neuron / Unit|计算输入加权和、偏置及激活的基本单元。|neuron|a=ReLU(w·x+b)；它是数学单元，不等同于生物神经细胞。
感知机|Perceptron|经典的线性阈值分类模型及其学习规则。|neuron|单个线性阈值不能分开 XOR；现代 MLP 通常使用可微训练。
仿射变换|Affine transformation|线性变换再加平移项。|neuron|Wx+b 在 b 非零时严格说是仿射变换。
导数|Derivative|一元函数在某点的局部变化率。|neuron|f(x)=x² 的导数是 2x；在 x=3 处为 6。
偏导数|Partial derivative|固定其他变量时，函数对某个变量的变化率。|descent|L(w,b) 对 w 的偏导表示只改变 w 的局部影响。
全连接层|Dense layer / Fully connected layer|每个输出单元与上一层所有输入相连的一层。|neuron|48→3 层含 144 条权重连接与 3 个偏置。
多层感知机|Multilayer perceptron / MLP|由全连接层与非线性激活组合成的前馈网络。|playground|2→8→8→1 网络用 tanh 隐藏层学习 XOR。
激活值|Activation|一个单元或层经过激活函数后的输出数值。|neuron|z=−2 时，ReLU 激活值为 0；z 本身是激活前值。
Logit|Logits / Log-odds|二分类中指对数几率；多类模型中常指 Softmax 前的未归一化分数。|loss|[-1,2,0] 可以是三类 logits；分数不必在 0–1 内。
二元交叉熵|Binary cross-entropy / BCE|二分类概率与标签之间的负对数损失。|loss|y=1 时为 −ln p，y=0 时为 −ln(1−p)。
负对数似然|Negative log-likelihood / NLL|模型对观测结果给出的概率取负对数，再求和或平均。|loss|正确类概率 0.5 时单样本 NLL≈0.693。
熵|Entropy|给定概率分布的平均信息量或不确定性度量。|loss|等概率二分类的自然对数熵是 ln2；确定分布为 0。
KL 散度|Kullback–Leibler divergence|衡量一个分布相对另一个分布的差异，通常不对称。|loss|H(P,Q)=H(P)+KL(P∥Q)，P 固定时最小化交叉熵与 KL 等价。
概率分布|Probability distribution|给离散结果分配非负且总和为 1 的概率，或用密度描述连续变量。|loss|Softmax 的三项输出共同构成离散类别分布。
梯度下降|Gradient descent / GD|沿目标函数负梯度方向更新参数的优化方法。|descent|w←w−η∂L/∂w；步长过大可能越过局部低点。
小批量|Mini-batch|一次梯度更新使用的一部分训练样本。|descent|180 条训练数据、batch=16 时最后一批有 4 条。
迭代步|Optimization step / Iteration|一次参数更新；不一定等于一轮遍历数据。|descent|12 次 step 可构成训练场 batch=16 的一个 epoch。
动量|Momentum|保留近期梯度方向的累计信息以影响当前更新。|descent|连续同向梯度会被延续，也可能让轨迹过冲。
二阶矩|Second moment|随机量平方的期望或滑动平均；不同于二阶导数。|descent|Adam 维护平方梯度的滑动平均来缩放更新。
偏差校正|Bias correction|修正滑动平均从零开始导致的初期偏小。|descent|Adam 用 m/(1−β₁ᵗ) 修正一阶矩。
AdamW|Adam with decoupled weight decay|把权重衰减与 Adam 梯度自适应缩放分离的优化方法。|descent|当前训练场实现的是把 L2 加入梯度的 Adam，不是 AdamW。
学习率调度|Learning-rate schedule|按训练进度或指标调整学习率的规则。|descent|先较大步长探索，再逐渐降低；本站暂不提供调度控件。
梯度裁剪|Gradient clipping|限制梯度值或梯度范数以控制过大的更新信号。|backprop|范数裁剪把超限梯度整体缩放，常用于防止爆炸性更新。
梯度消失|Vanishing gradient|反向传递时梯度逐渐缩小到难以有效学习的现象。|backprop|多次乘小于 1 的激活导数可能使深层路径信号衰减。
梯度爆炸|Exploding gradient|反向传递中梯度幅度过度增大的现象。|backprop|连续放大使更新不稳定；应检查尺度、初始化和优化设置。
自动微分|Automatic differentiation / Autodiff|根据实际运算及导数规则执行链式法则的求导方法。|backprop|不是用极小步长逐个试参数，也不要求输出符号公式。
数值差分|Finite difference|用邻近位置的函数值近似导数。|backprop|(f(w+h)−f(w−h))/(2h) 可检查一个权重的解析梯度。
梯度累积|Gradient accumulation|先把多个微批次的梯度合并，再执行参数更新。|backprop|用小内存模拟较大批量时，要保持损失平均口径一致。
Xavier 初始化|Glorot / Xavier initialization|按输入与输出宽度控制初始权重方差的方法。|playground|常用于 tanh 等网络，避免各层激活尺度很快失控。
He 初始化|Kaiming / He initialization|针对 ReLU 类激活设计、考虑输入宽度的初始化。|playground|考虑一部分激活被截为 0，调整初始权重尺度。
随机种子|Random seed|决定伪随机序列起点的整数。|playground|相同算法与种子可重现数据、初始权重及批次顺序。
测试集|Test set|模型和方案选定后用于最终评价、未参与选择的数据。|generalization|反复依据测试分数调参，会削弱它的独立性。
欠拟合|Underfitting|模型或训练不足以捕捉任务规律的现象。|generalization|用一条直线拟合明显弯曲的关系，训练误差也很大。
数据泄漏|Data leakage|训练或选择过程不当地使用了预测时不该知道的信息。|generalization|先用全量数据选特征再划测试集，可能让评价过于乐观。
交叉验证|Cross-validation|多次轮换数据子集承担训练和验证角色的评价方法。|generalization|五折验证各留一折评估；相关个体与时间顺序仍需考虑。
早停|Early stopping|根据验证表现停止训练并选择先前较好的参数。|generalization|验证损失持续上升时，可回到较早的检查点。
Dropout|Dropout|训练时随机屏蔽部分激活的正则化方法。|generalization|常见实现按保留概率补偿尺度，推断时关闭屏蔽。
L1 正则化|L1 regularization|在目标中加入权重绝对值之和，常促进稀疏。|generalization|λΣ abs(w) 与 L2 的 λΣw² 形状不同。
L2 正则化|L2 regularization / Weight decay|在目标中加入平方权重惩罚；与权重衰减的等价依赖优化器。|generalization|在自适应优化器中，加入 L2 梯度通常不等于解耦衰减。
分布偏移|Distribution shift / OOD|使用阶段的数据分布与训练分布不同。|generalization|只见过线条的分类器遇到圆圈仍输出三类概率，但未获相应训练。
准确率|Accuracy|预测正确的样本数占全部样本的比例。|metrics|90% 负类任务全部猜负也能有 90% 准确率。
分类阈值|Decision threshold|把预测分数或概率变成类别决定的界限。|metrics|阈值从 0.5 降到 0.2，通常会报出更多正类。
类别不平衡|Class imbalance|不同类别的样本数量或比例差别较大。|metrics|少数类仅 1% 时，需要同时关注召回、精确率与错误代价。
概率校准|Probability calibration|预测概率与长期实际发生频率是否一致。|metrics|给出 0.8 的一组预测，理想情况下约 80% 为真。
ROC 曲线|Receiver operating characteristic / ROC|改变阈值时，以假阳性率为横轴、真阳性率为纵轴的曲线。|metrics|真阳性率即召回率；还应结合类比例和任务代价解读。
PR 曲线|Precision–recall curve|改变阈值时精确率与召回率的关系曲线。|metrics|在稀有正类任务中可直接观察“找得全”和“报得准”的权衡。
卷积神经网络|Convolutional neural network / CNN|使用可学习局部卷积、非线性等组件提取特征的神经网络。|cnn|本例只训练分类头；完整 CNN 通常也更新卷积核。
互相关|Cross-correlation|局部窗口与核对应相乘求和，不翻转核。|convolution|深度学习中的 Conv2d 通常采用这种运算约定。
通道|Channel|在同一空间或序列位置并列的特征轴。|convolution|RGB 有 3 个输入通道；隐藏层通道可表示学习到的响应。
特征图|Feature map|某个通道在各空间位置上的响应数组。|cnn|Sobel x 产生一张 8×8 图，显示不同位置的亮度差。
感受野|Receptive field|一个输出位置能够受到影响的输入区域。|convolution|两层 3×3、步幅 1 卷积的有效感受野为 5×5。
权重共享|Weight sharing|不同位置或调用共同使用同一组参数。|convolution|3×3 核滑过整张图时不为每个窗口另建 9 个参数。
膨胀卷积|Dilated convolution / Dilation|在卷积核采样位置之间按间隔取输入的卷积。|convolution|3×3 核、膨胀率 2 覆盖 5×5 区域；本站基础控件未实现膨胀。
最大池化|Max pooling|对局部窗口取最大值进行汇总。|cnn|窗口 [1,2;3,4] 的最大池化结果为 4。
平均池化|Average pooling|对局部窗口取平均值进行汇总。|cnn|窗口 [1,2;3,4] 的平均池化结果为 2.5。
全局平均池化|Global average pooling / GAP|对一个通道的所有空间位置求平均，通常每通道留一个数。|cnn|3×4×4 经 GAP 得到 3 个数；本例用展平保留 48 个数。
展平|Flatten|按约定顺序把多个轴排成一个向量，不改变数值本身。|cnn|3×4×4 展为 48；展平没有训练参数。
分类头|Classification head|把特征映射为任务类别分数的输出模块。|cnn|本例是 48→3 全连接加 Softmax，共 147 个可训练参数。
冻结参数|Frozen parameters|训练中保持不变、不会被优化器更新的参数。|cnn|本例三片卷积核被冻结；分类头权重会变化。
数据增强|Data augmentation|通过变换已有样本构造训练变化、提高适用范围的方法。|cnn|小范围平移或加噪声需保持标签语义；旋转可能把竖线变成横线。
迁移学习|Transfer learning|把源任务或数据上学到的表示用于另一个任务。|cnn|真实预训练特征可被冻结再训练新头；本例核为人工设计，不称预训练迁移。
注意力|Attention|用输入相关的权重对一组值向量进行汇总的机制。|attention|对 V 的加权和才是输出，不是仅显示一张匹配热图。
自注意力|Self-attention|Q、K、V 来自同一序列的注意力。|attention|一句话内部各位置互相读取信息。
交叉注意力|Cross-attention|Q 来自一个序列、K/V 来自另一个序列的注意力。|attention|解码器查询编码器产生的源序列表示。
缩放点积注意力|Scaled dot-product attention|把 QKᵀ 按 √dₖ 缩放，经掩码与 Softmax 后加权 V。|attention|键向量宽度是 2 时，分数除以 √2。
多头注意力|Multi-head attention / MHA|在多个投影子空间独立计算注意力再拼接投影。|transformer|总宽度 4、两头时每头宽度 2，不把头的概率直接平均当输出。
位置编码|Positional encoding / PE|向模型提供顺序或位置信息的表示机制。|transformer|本例正弦编码按位置计算，不需要学习参数。
残差连接|Residual connection / Skip connection|把输入通过加法捷径传到后续输出。|transformer|X+Attention(X) 要求形状相同；它与预测残差不同。
层归一化|Layer normalization / LayerNorm / LN|通常按单个样本或词元的特征维归一化，再使用仿射参数。|transformer|本例每行 4 个特征各自算均值与方差，不跨词元。
批量归一化|Batch normalization / BatchNorm / BN|按特征/通道利用批次及适用空间轴的统计量归一化。|transformer|与每行独立的 LayerNorm 不同，训练和推断的统计处理也不同。
前馈网络|Feed-forward network / FFN|信息沿输入到输出方向计算、无循环反馈的网络。|transformer|Transformer 中的逐位置 FFN 对各行共享同一组 4→8→4 权重。
Post-LN|Post-layer normalization|残差相加之后执行 LayerNorm 的块安排。|transformer|本例 H=LN(X+MHA(X))，再经过前馈子层。
Pre-LN|Pre-layer normalization|先对分支输入归一化，再计算子层和残差相加的安排。|transformer|常见形式 H=X+MHA(LN(X))；不能与 Post-LN 公式混用。
编码器|Encoder|把输入转换为表示的模块；具体注意力约束由架构规定。|transformer|原始 Transformer 编码器使用非因果自注意力。
解码器|Decoder|根据已有表示或上下文产生目标输出的模块。|transformer|原始解码器含因果自注意力和交叉注意力；仅解码器变体结构不同。
分词器|Tokenizer|在文本与词元编号序列之间转换的组件。|tokens|真实分词器不保证一个汉字就是一个 token。
词表|Vocabulary|词元及其编号的集合。|tokens|专题只用 8 项词表，实际语言模型词表通常大得多。
BPE|Byte-pair encoding|反复合并高频相邻符号对来构造子词单元的方法。|tokens|学得的合并规则和基础符号决定切分；字节级 BPE 是一种具体变体。
自回归|Autoregressive|把联合分布分解为依次依赖此前结果的条件分布。|tokens|p(x₁…xₙ)=∏p(xᵢ∣x₁,…,xᵢ₋₁)，生成时把新 token 接回上下文。
上下文窗口|Context window|模型一次处理时能使用的词元范围或长度限制。|tokens|本实验人为上限 8；它不是通用模型能力上限。
贪心解码|Greedy decoding / Argmax|每次直接选择当前分数最高的候选。|tokens|top-k=1 等价于只保留最高项，再采样没有随机选择余地。
top-p|Nucleus sampling / Top-p|保留累计概率达到 p 的最小高概率候选前缀后采样。|tokens|[0.5,0.3,0.2] 且 p=0.7 时保留前两项并重新归一化。
困惑度|Perplexity / PPL|平均负对数似然取指数的序列模型指标。|tokens|自然对数平均损失为 ln2 时，PPL=2。
教师强制|Teacher forcing|训练时把真实历史输出作为后续位置输入的做法。|tokens|训练预测 C 时提供真实 A、B；生成时历史来自模型已选结果。
结束词元|End-of-sequence / EOS|标记序列结束的特殊词元。|tokens|生成器选到 EOS 时可停止；句号不必等价于 EOS。
聚类|Clustering|按照某种相似性或目标把无标签样本组织成组。|clustering|聚类编号是算法结果，需要结合研究问题解释。
K-means|K-means / Lloyd algorithm|交替最近中心分配与均值更新，最小化簇内平方和。|clustering|选 K=3 不代表数据天然只有三类。
K-means++|K-means++ initialization|按到已有中心的最近平方距离抽样新中心的初始化。|clustering|倾向于分散起点，但不保证全局最优。
质心|Centroid / Cluster center|一组点的坐标均值。|clustering|[0,0] 和 [2,4] 的质心是 [1,2]，不必是原始样本。
簇内平方和|Inertia / Within-cluster sum of squares / WCSS|每个点到所属中心的平方距离之和。|clustering|固定 K 和尺度才容易比较；增加中心往往让它下降。
欧氏距离|Euclidean distance|坐标差平方和的平方根。|clustering|[0,0] 与 [3,4] 的欧氏距离是 5，平方距离为 25。
局部最优|Local optimum|在邻近可达变化中难以进一步改善、未必全局最好的解。|clustering|不同初始中心可能使 K-means 停在不同分组。
收敛|Convergence|迭代状态或目标逐步接近稳定结果的性质或停止依据。|clustering|中心不再变化是本例停止条件；收敛不等于找到真实类别。
`.trim().split('\n').map(line => {
    const fields = line.split('|');
    if (fields.length !== 5) throw new Error('词条需为五列：' + fields[0]);
    const [term, en, definition, chapter, example] = fields;
    return { term, en, definition, chapter, example };
  });
  C.glossary.push(...rows);
  C.glossary.push(
    {term: 'Transformer', en: 'Transformer', definition: '以注意力、逐位置前馈、残差和归一化等模块组合表示序列的架构族。位置处理、掩码和块的排列方式随变体变化。', chapter: 'transformer', example: '本例包含一个 Post-LN 自注意力块和词表头，最多 8 个词元、4 维表示；所有权重固定，完整计算前向但不具备训练后的语言能力。'},
    {term: '卷积', en: 'Convolution', definition: '用局部核在空间位置共享参数的运算。深度学习常用的实现实际是不翻转核的互相关，并把多个输入通道的响应汇总为输出通道。', chapter: 'convolution', example: '8×8 输入、3×3 核、步幅 1、填充 1 产生 8×8 输出；同一片核在所有位置使用同一组权重。'}
  );
  const examples = {
    '人工智能': '可以包含搜索、规则推理或学习系统；不是所有 AI 都使用神经网络。',
    '机器学习': '逻辑回归、决策树、K-means 与神经网络都可以从数据得到模型或结构。',
    '深度学习': '重点在多层表示学习；模型层数多不保证当前任务效果更好。',
    '自监督学习': '例如把文本向后错一位产生下一个词元标签；没有人工逐条标注不等于没有学习目标。',
    '标量': '一个学习率 0.03 是标量；一组学习率组成向量。', '向量': '二维点 [0.3,−0.7] 是一个向量，分量含义由任务定义。',
    '矩阵': '把 10 个二维点按行堆起来得到 10×2 数据矩阵。', '张量': '数学中有更一般的定义；深度学习实现中常指带形状的多维数组。',
    '特征': '原始坐标、词向量或卷积响应都可作为特征，不要求每维都有人工名称。', '标签': '0/1 是约定的类别编码，不表示两类存在数值大小关系。',
    '权重': 'z=2x+1 中的 2 是权重；改它会改变输入对输出的影响。', '偏置': 'z=2x+1 中的 1 是偏置，让函数在 x=0 时也可非零。',
    '激活函数': 'ReLU 把负数设为 0，tanh 把输入映射到 −1 与 1 之间。', 'Sigmoid': 'z=0 时输出 0.5、导数 0.25；二分类常用它把 logit 变成概率。',
    'ReLU': 'ReLU(−2)=0、ReLU(3)=3；0 点的导数需要实现约定，本项目取 0。', 'tanh': 'tanh(0)=0、0 点导数为 1；绝对值很大时趋于饱和。',
    '损失函数': '单次预测的坏程度用损失度量；批次中还需要约定求和或平均。', '均方误差': '误差为 1 和 3 时 MSE=(1²+3²)/2=5。',
    '交叉熵': '真实类别概率从 0.1 提升到 0.9 时，单样本损失由约 2.303 降为 0.105。', 'Softmax': '给全部 logits 加同一个常数，概率不变；多类概率之和为 1。',
    '梯度': 'L(w,b) 的梯度是 [∂L/∂w,∂L/∂b]，不是只有一个方向标记。', '学习率': '梯度为 −2，学习率 0.1，SGD 会把该参数增加 0.2。',
    '批量': '一批可以是 16 条训练样本；批量大小影响一次更新用到多少数据。', '轮次': '遍历训练数据一遍是一轮，内部可能有多次参数更新。',
    '前向传播': '输入→线性变换→激活→输出→损失，各中间值用于后续求导。', '反向传播': '先求损失对输出的导数，再沿图反向合成各参数梯度；尚未更新参数。',
    '链式法则': 'L(a(z(w))) 对 w 的导数为三个局部导数相乘，多条路径再相加。', '计算图': '节点可表示变量或运算，连线表示数值依赖，不是网络拓扑的装饰图。',
    '隐藏层': '位于输入与最终输出之间的表示层；层内单元数量称为宽度。', '初始化': '训练前给参数起始值；隐藏单元完全相同的权重可能妨碍打破对称。',
    'SGD': '逐样本或小批量梯度是完整数据梯度的估计；术语使用要结合批量约定。', 'Adam': '维护一阶矩与平方梯度二阶矩，按参数分量自适应缩放更新。',
    '训练集': '训练场的 180 条样本参与梯度，60 条验证样本不参与。', '验证集': '适合比较设置，反复查看意味着它参与了模型选择。',
    '过拟合': '模型拟合到训练噪声，训练误差下降而验证表现变差是一种信号。', '正则化': '用约束或惩罚表达偏好，限制复杂度或提高稳定性，不保证每次都更优。',
    '混淆矩阵': '按真实与预测标签交叉计数；读表前先看行列对应哪一种。', '精确率': '报出的 8 个正类中有 6 个真阳性，精确率是 75%。',
    '召回率': '真实 10 个正类找回 6 个，召回率是 60%。', 'F1 分数': '等于 2TP/(2TP+FP+FN)，兼顾精确率与召回率，但不计 TN。',
    '卷积核': '一片 3×3 核只有 9 个数，滑动时共享，不随图像大小增加。', '步幅': '步幅从 1 改为 2，会跳过部分位置并改变输出尺寸。',
    '填充': '零填充在边界加 0；输入边界可能因此产生人工亮度差。', '池化': '最大/平均池化都汇总局部值；它们不是在学习新的卷积权重。',
    '查询': '每一行 Q 提问“该从哪些位置读取”；这是计算角色的类比，不是自然语言问句。', '键': 'K 参与匹配分数；因果掩码之后，未来位置不会获得权重。',
    '值': '注意力权重最终乘在 V 上；V 不是 Softmax 概率。', '因果掩码': '当前位置可以看自己及更早位置；用 −∞ 分数可使未来位置 Softmax 权重为 0。',
    '词元': '同一文本在不同分词器下可能得到不同数量和边界的 token。', '嵌入': '编号 3 对应嵌入表第 3 项；编号差 1 不代表语义距离近。',
    '温度': 'T>1 通常更分散、0<T<1 更集中；不改变最高 logit 的排序。', 'top-k': 'k=1 只剩最高分项；它限制候选集合，不更新模型权重。'
  };
  for (const term of C.glossary) {
    term.example ||= examples[term.term];
    term.refs ||= C.chapters.find(c => c.id === term.chapter).refs.slice(0, 2);
  }
  const related = [
    ['参数', '超参数', '权重', '偏置'], ['标量', '向量', '矩阵', '张量', '形状'],
    ['点积', '矩阵乘法', '逐元素乘法', '转置'], ['监督学习', '无监督学习', '自监督学习', '强化学习'],
    ['训练', '推断', '前向传播', '反向传播'], ['Logit', 'Sigmoid', 'Softmax', '交叉熵', '二元交叉熵'],
    ['损失函数', '目标函数', '准确率'], ['梯度', '偏导数', '链式法则', '自动微分', '数值差分'],
    ['轮次', '批量', '小批量', '迭代步'], ['SGD', 'Adam', 'AdamW', '动量'],
    ['训练集', '验证集', '测试集', '数据泄漏'], ['过拟合', '欠拟合', '早停', 'Dropout', 'L2 正则化'],
    ['精确率', '召回率', 'F1 分数', '混淆矩阵'], ['卷积', '卷积核', '通道', '特征图', '感受野'],
    ['池化', '最大池化', '平均池化', '全局平均池化', '展平'], ['分类头', '冻结参数', '迁移学习'],
    ['查询', '键', '值', '自注意力', '交叉注意力'], ['Transformer', '多头注意力', '位置编码', '残差连接', '前馈网络'],
    ['层归一化', '批量归一化', 'Pre-LN', 'Post-LN'], ['词元', '分词器', 'BPE', '词表', '嵌入'],
    ['温度', 'top-k', 'top-p', '贪心解码'], ['聚类', 'K-means', 'K-means++', '质心', '簇内平方和']
  ];
  for (const term of C.glossary) term.related = [...new Set(related.filter(group => group.includes(term.term)).flat())].filter(x => x !== term.term);
  C.termLink = term => '#/glossary/' + encodeURIComponent(term);
  C.normalizeSearch = text => String(text).toLowerCase().replace(/[\s_\-–—]+/g, '');
  C.findTerms = (query = '', chapter = '') => {
    const q = C.normalizeSearch(query);
    return C.glossary.filter(t => (!chapter || t.chapter === chapter) && C.normalizeSearch([t.term, t.en, t.definition, t.example].join(' ')).includes(q));
  };
  if (typeof module !== 'undefined' && module.exports) module.exports = C;
})(globalThis);
