/* Advanced LLM track: original explanations and examples; primary-source reading map. */
(function (root) {
  'use strict';
  const C = root.AI.C;
  const sources = [
    ['llm-d2l', 'D2L · Language Models', 'https://d2l.ai/chapter_recurrent-neural-networks/language-model.html', '联合分布、自回归分解、困惑度与语言建模基础'],
    ['llm-pretrain', 'D2L · Large-Scale Pretraining with Transformers', 'https://d2l.ai/chapter_attention-mechanisms-and-transformers/large-pretraining-transformers.html', '编码器、解码器、预训练与上下文学习'],
    ['llm-bpe', 'Hugging Face LLM Course · BPE', 'https://huggingface.co/learn/llm-course/chapter6/5', '统计相邻符号、学习合并规则与应用分词器'],
    ['llm-rope', 'Su et al. · RoFormer (2021/2023)', 'https://arxiv.org/abs/2104.09864', '旋转位置编码与相对位置的点积结构'],
    ['llm-rms', 'Zhang & Sennrich · RMSNorm (2019)', 'https://arxiv.org/abs/1910.07467', '按均方根缩放与省略重新中心化'],
    ['llm-glu', 'Shazeer · GLU Variants (2020)', 'https://arxiv.org/abs/2002.05202', 'SwiGLU 门控前馈网络'],
    ['llm-cache', 'Transformers · Cache strategies', 'https://huggingface.co/docs/transformers/en/kv_cache', '缓存过去的键值、缓存增长与实现选择'],
    ['llm-gqa', 'Ainslie et al. · GQA (2023)', 'https://arxiv.org/abs/2305.13245', '查询头与共享键值头之间的关系'],
    ['llm-flash', 'Dao et al. · FlashAttention (2022)', 'https://arxiv.org/abs/2205.14135', '精确注意力的分块与内存读写优化'],
    ['llm-scaling', 'Hoffmann et al. · Compute-Optimal LMs (2022)', 'https://arxiv.org/abs/2203.15556', '模型规模、训练数据量与计算预算的联合选择'],
    ['llm-sft', 'TRL · SFT Trainer', 'https://huggingface.co/docs/trl/en/sft_trainer', '指令样本、响应损失掩码、packing 与微调'],
    ['llm-chat', 'Transformers · Chat templates', 'https://huggingface.co/docs/transformers/en/chat_templating', '多轮角色消息的序列化与特殊词元'],
    ['llm-lora', 'Hu et al. · LoRA (2021)', 'https://arxiv.org/abs/2106.09685', '冻结基座、低秩更新、参数与合并'],
    ['llm-qlora', 'Dettmers et al. · QLoRA (2023)', 'https://arxiv.org/abs/2305.14314', '量化基座与低秩微调，不等于全参数低位训练'],
    ['llm-dpo', 'TRL · Direct Preference Optimization', 'https://huggingface.co/docs/trl/en/dpo_trainer', '相对参考策略的偏好损失与实际公式'],
    ['llm-rlhf', 'Ouyang et al. · InstructGPT (2022)', 'https://arxiv.org/abs/2203.02155', '示范、偏好奖励和策略优化的阶段区分'],
    ['llm-grpo', 'Shao et al. · DeepSeekMath (2024)', 'https://arxiv.org/abs/2402.03300', '组相对策略优化及可检验任务的奖励'],
    ['llm-quant', 'Transformers · Quantization concepts', 'https://huggingface.co/docs/transformers/en/quantization/concept_guide', '低位表示、缩放、分组与精度损失'],
    ['llm-moe', 'Fedus et al. · Switch Transformers (2021)', 'https://arxiv.org/abs/2101.03961', '稀疏专家、路由、容量与负载均衡'],
    ['llm-rag', 'Lewis et al. · Retrieval-Augmented Generation (2020)', 'https://arxiv.org/abs/2005.11401', '参数知识与检索证据结合的建模思路'],
    ['llm-eval', 'Liang et al. · HELM (2022)', 'https://arxiv.org/abs/2211.09110', '跨场景、多指标、可复现实验与评价盲区'],
    ['llm-vit', 'D2L · Transformers for Vision', 'https://d2l.ai/chapter_attention-mechanisms-and-transformers/vision-transformer.html', '图像 patch、线性投影与序列输入'],
    ['llm-serving', 'Kwon et al. · PagedAttention (2023)', 'https://arxiv.org/abs/2309.06180', '服务并发、KV 内存管理与吞吐'],
    ['llm-book', '动手学深度学习 · 中文开源教材', 'https://zh.d2l.ai/', '作为数学、序列模型、注意力、优化与工程的延伸阅读主线']
  ];
  C.sources.push(...sources.map(([id,title,url,use]) => ({id,title,url,use})));
  const start = C.chapters.length;
  const groups = [
    ['语言与表示', '先把数据、目标和内部计算对应起来', 6],
    ['训练与适配', '从可训练小模型走向预训练与后训练', 5],
    ['推理与应用', '理解生成、压缩、检索、评估和服务', 7]
  ];
  let cursor = start + 1;
  groups.forEach(([title,subtitle,count]) => { C.groups.push({title,subtitle,range:`${cursor}—${cursor+count-1}`,track:'llm'}); cursor += count; });
  const add = (id,short,title,lead,challenge,prereqs,refs,body,formula,detail,exercise,quiz) => {
    const offset = C.chapters.length - start;
    C.chapters.push({id,short,title,lead,summary:body[0][1].split('。')[0]+'。',challenge,
      group:4+(offset<6?0:offset<11?1:2),track:'llm',prereqs,refs,
      body:body.map(([h,p])=>({h,p})),
      deep:{title:'展开公式、尺寸与一项迁移练习',html:`<div class="formula">${formula}</div><p>${detail}</p><div class="llm-exercise"><strong>迁移练习</strong><p>${exercise}</p></div>`},
      quiz:{q:quiz[0],options:quiz[1],answer:quiz[2],why:quiz[3]}
    });
  };
  add('llm-objective','语言建模与训练目标','一句话，为什么能变成多道预测题？',
    '从联合概率、自回归分解和输入与标签移位开始，把训练张量、因果约束、损失与困惑度接在一起。',
    '把某个位置的正确词概率调低，再关闭该位置的损失。解释两次操作为什么对平均损失产生不同影响。',
    ['loss','tokens'],['llm-d2l','llm-pretrain'],[
      ['先明确语言模型学的是什么','语言模型给词元序列建立概率模型。自回归分解把整句概率写成每个词元在前文条件下的概率乘积；取对数后变为求和。训练中提高真实续文的概率，推断中则从条件分布选择一个词元再接回去。这一统计任务为许多能力提供基础，却没有单独保证事实正确或遵循指令。'],
      ['输入与标签只错开一个位置','序列为 BOS、我、爱、猫、EOS 时，输入是 BOS、我、爱、猫，标签是我、爱、猫、EOS。长度同为四，但每一对含义是“读到这里后，下一个是什么”。整批 logits 常为 B×T×V，标签为 B×T。API 可能内部完成移位，也可能要求外部处理；重复移位会让目标错位。'],
      ['训练并行与生成串行可以同时成立','训练时整段真实文本已经存在，因果掩码让位置 t 只读取位置 ≤t，可并行计算各位置的预测。生成时未知的新词尚未产生，必须先选出再继续。训练的教师强制与生成自己的前缀是两种输入分布；一次前向得到很多位置的概率，不意味着未来续文可以无条件一次选完。'],
      ['注意力掩码与损失掩码控制不同环节','注意力掩码决定能读取哪些位置；损失掩码决定哪些预测计入目标。把用户问题的 loss mask 设为 0，回答仍可以读取问题；它也可能通过回答的梯度影响表示学习。Padding、跨文档注意力和 assistant-only loss 的规则应分别检查，不能用一个“mask”字样糊在一起。'],
      ['平均损失的分母决定可比性','本实验只对 mask=1 的有效词元平均负对数概率，全部屏蔽时显示未定义。困惑度为 exp(平均 NLL)。不同分词器会改变词元边界及分母，所以 PPL 适合在同语料、同分词口径下比较。实验概率由你设定，用于手算，不来自一个语言模型。']
    ],'p(x₁…xT)=∏ₜp(xₜ | x&lt;t)<br>L=−Σₜ mₜ log p(xₜ | x&lt;t) / Σₜmₜ<br>PPL=exp(L)',
    '这里的对数是自然对数，p 是正确标签的概率而非最大类别概率。注意力矩阵通常为 B×H×T×T；输出 logits 为 B×T×V，两个最后一维承担不同含义。',
    '四个位置正确概率为 0.5、0.25、0.5、0.25，先计算全部词元的 PPL，再只保留第 1、3 项。分别写出求和项和分母。',
    ['屏蔽用户问题的损失后，回答还能读取问题吗？',['不能，问题被删除了','可以，能否读取由注意力和输入决定','只能读取最后一个字'],1,'损失掩码不删除输入，也不自动改变注意力可见性。']);
  add('llm-bpe','分词器与 BPE','词元边界，也是在数据中学出来的。',
    '亲手学习几条相邻符号合并规则，再观察词元数量、可逆性与词表之间的取舍。',
    '先在重复词上合并，再加入只出现一次的新词。查看最常见相邻对和非重叠替换次数是否总相同。',
    ['tokens'],['llm-bpe','llm-d2l'],[
      ['分词器不是按词义自动划分','分词器把文本变成离散符号序列，其编号随后进入嵌入表。BPE 的训练依赖相邻符号频率，并不先理解哪个片段是一个完整概念。一个专有名词可能被切成多个子词，代码、中文和罕见字符的拆分也可能不同。比较 token 数量时必须指定分词器。'],
      ['从字符到合并规则','本实验从 Unicode 码点开始，统计不跨空白的相邻对，选出现最多的一对并做从左到右的非重叠替换。频率相同按符号键排序以保证复现。只有至少出现两次的对才合并，最多 24 次；这是明确的字符级 BPE 变体，不是任何商业模型的实际 tokenizer。'],
      ['学习规则与使用规则要分开','真实部署先在训练语料上学习有序合并规则和词表，再把规则应用于新文本。本站显示的是小语料的规则学习过程，编辑语料就重新训练分词过程。由 BPE 训练得到 token 列表不等于 LLM 已学会这些片段的含义；嵌入仍要经后续模型训练。'],
      ['压短序列也会付出词表代价','较大的词表可能减少序列长度，却增加 V×d 嵌入参数和输出投影的计算。较小词表能表达更细的片段，序列却可能更长。实验中的“当前唯一片段数”不是完整 tokenizer 词表大小：真实词表还包含基础符号、历史合并与特殊词元。'],
      ['把空白与字节处理说清楚','这里保留空格和换行作为独立边界，所有片段拼接可恢复原文；表中以可见符号显示它们。字节级 BPE 在字节字母表上操作，SentencePiece 还包含不同的预处理与 Unigram 等方法。解码可逆性不等于训练中的 Unicode 规范化永远不丢信息。']
    ],'pair*=argmax count(a,b)<br>(a,b) → ab<br>decode(pieces)=pieces.join("")',
    '在 aaaa 中，相邻 aa 的计数是 3，但一次非重叠合并只替换两处。界面把计数与实际替换次数分开，避免把频率误当作必然节省的 token 数。',
    '把“学习模型 学习模型 学习”逐步合并，记录每次规则；再讨论训练语料没有的符号需要字节回退还是 UNK。',
    ['一次 BPE 合并意味着模型学会这个词的语义了吗？',['已经形成语义向量','只学习到一种频率驱动的编码规则','已经完成指令微调'],1,'分词器确定符号边界；语言模型随后学习嵌入与上下文表示。']);
  add('llm-data','语料、去重与 Packing','数据管线，决定模型每天在学什么。',
    '编辑小语料，检查文档去重、EOS、补齐与窗口打包。用真实格子占用解释利用率。',
    '加入重复行，比较去重前后的有效 token 数；改变窗口长度，观察最后一个窗口的空位。',
    ['llm-objective','llm-bpe'],['llm-pretrain','llm-sft','validation'],[
      ['语料质量与目标分布一起设计','预训练语料涉及语言、领域、年代与文体的混合。去重、格式清理、质量筛选和隐私处理影响实际学到的分布；高频不自动等于高质量。合成数据也应检查来源、重复、错误传递和与评价集的重合。下采样某个领域会改变能力覆盖，不能只看总 token 数。'],
      ['去重有多个粒度','本实验只按每行 trim 后的完整字符串做精确去重。真实数据还需文档、段落及近重复检查，可能使用哈希、MinHash 等方法。重复会占用预算、扩大某些样本权重并污染评价；过度去重也可能误删不同语境中的有效信息。UI 的删重数仅描述这一简化规则。'],
      ['打包解决长度浪费，不自动解决样本隔离','给每篇文档加 EOS，再连续填入固定窗口，可减少一文档一窗口造成的 Padding。EOS 是符号而非硬注意力屏障。若目标要求不同文档不能互相读取，要使用文档边界构造块对角因果掩码，或其他明确策略；窗口内颜色只是显示文档来源。'],
      ['跨窗口与跨文档需要分别处理','文档可能被截成多个窗口，后一段是否继承前段上下文会改变目标。多篇文档拼在一个窗口，又可能产生跨文档预测。实验仅展示字符加 EOS 的布局，不训练模型，也没有把图中的颜色当作实际隔离代码。Padding 在损失中应排除，其注意力可见性也要配置。'],
      ['先划分评价数据，再拟合处理规则','研究任务中，测试污染不仅来自文本完全重复，也可能来自同一道题的改写与答案泄漏。保留可审计的数据版本、过滤日志和来源记录。没有授权的文本不能因为网页可访问就被假定可自由分发；本站全部预设句子原创，不导入第三方语料。']
    ],'有效利用率=非 PAD 位置数 / 总分配位置数<br>同文档因果可见：doc(i)=doc(j) 且 j≤i',
    '字符只是此例的布局单位，不是某个 LLM 的真实 token 计数。实际 tokenizer、EOS 编码、窗口重叠与文档边界策略都会改变数据量和训练分母。',
    '三个文档长度分别为 3、5、7，各补一个 EOS，窗口长 8。手算一文档一窗口与连续 packing 的占用，并写出不能跨文档读取时的 mask。',
    ['仅加入 EOS 能强制阻止跨文档注意力吗？',['能，EOS 是物理屏障','不能，还要看注意力掩码与打包策略','只有中文可以'],1,'EOS 告诉模型一个边界；硬可见性由计算规则决定。']);
  add('llm-rope','RoPE 与长上下文','把位置变成旋转，点积就带上距离。',
    '旋转二维子空间，检查向量长度与相对位置关系；再把实验延伸到长上下文的限制。',
    '把查询位置和键位置同时加相同偏移，检查点积是否保持；再只移动其中一个位置。',
    ['vectors','attention','transformer'],['llm-rope','position'],[
      ['RoPE 改变 Q 与 K 的坐标','旋转位置编码把 Q/K 的相邻分量配成二维平面，按位置乘一个旋转矩阵。多个平面使用不同频率。通常不直接旋转 V；最终效果进入注意力的匹配分数。位置编码有多种实现，这里采用相邻配对约定，旋转图只显示第一对分量。'],
      ['相对位置从矩阵乘法中出现','同一频率下，旋转后的点积可写为 qᵀR(n−m)k。因此共同平移 m、n 不改变该项点积，只改变相对位移才改变分数。这里讨论相同底数、相同旋转约定与同一对原始向量；完整模型还包含其他位置依赖与上下文内容，不能把恒等式扩大为输出恒定。'],
      ['旋转保长度，不创造语义','正交旋转保持欧氏长度，角度和相对朝向发生变化。当前 Q/K 是人工小向量，图中靠近或远离不表示两个词语在语义上更相似。不同频率同时参与点积，不能仅凭第一平面的转角解释完整长距离注意力。'],
      ['位置可计算与长度可泛化不同','能计算训练范围外的位置角度，不保证模型在很长上下文中仍有效。位置插值、基频调整、滑动窗口等方案会改变频率、可见区域或训练条件，需要验证长距离检索与推理。长窗口也增加 KV 缓存，注意力内存优化不能自动延长学会的依赖范围。'],
      ['顺序、位置和证据利用率分开测','同样长度的输入可能有不同的关键信息位置。测试长上下文时，应移动证据、加入干扰、改变任务组合；仅把 max_position_embeddings 改大属于配置变化。RAG 把相关资料筛进窗口，解决的又是证据选择问题，不等价于扩展位置能力。']
    ],'R(θ)=[cosθ −sinθ; sinθ cosθ]<br>q′=R(mθ)q, k′=R(nθ)k<br>q′ᵀk′=qᵀR((n−m)θ)k',
    '4 维时两对角度频率分别为 1 与 base⁻¹ᐟ²。底数只控制不同平面的频率；本章不实现任何特定长上下文扩展算法。',
    '取 q=[1,0]、k=[0,1]，先手算 m=n 时点积，再令 n−m=π/2。用界面验证“长度不变但分数变化”。',
    ['同时给 m 与 n 加相同偏移，理想同频 RoPE 点积怎样变化？',['不变，因为只依赖相对位置','必然线性增加','向量长度翻倍'],0,'R(m)ᵀR(n)=R(n−m)，共同偏移抵消。']);
  add('llm-block','现代解码器部件','RMSNorm、SwiGLU 与残差，各改了什么？',
    '不用下载模型，直接修改四维向量，对比 LayerNorm、RMSNorm 和门控前馈的实际数值。',
    '给向量全部分量加相同常数，比较 LN 与 RMSNorm；把门控输入变负，观察 SwiGLU 是否像 ReLU 一样全归零。',
    ['transformer','neuron'],['llm-rms','llm-glu','llm-pretrain'],[
      ['现代解码器是一组可组合设计','一个常见设计是嵌入、重复的因果注意力块、最终归一化和词表投影。块内可采用 Pre-Norm、RoPE、GQA、SwiGLU 等选项，但这些不是所有模型的统一清单。读模型配置时先看隐藏宽度、层数、头数、前馈维度与词表，再核实公式和权重形状。'],
      ['RMSNorm 只做尺度处理','LayerNorm 先减均值再按方差缩放；RMSNorm 直接除以均方根，不做重新中心化。两者通常有可学习的缩放参数，LN 还常有偏移项。本实验固定增益为 1、偏移为 0，方便看到区别：给全部分量加常数后，LN 近似不变，RMSNorm 一般改变。'],
      ['SwiGLU 的门不是二元开关','SwiGLU 用两条投影分支：一条经 SiLU(x)=x·sigmoid(x)，再与另一条逐元素相乘，最后向下投影。SiLU 的负半轴可以非零，因此它不等于 ReLU 截断。门控的实际数值和语义由训练决定；本实验用四个人工分量，把乘法因素分开展示。'],
      ['残差与归一化排列需要按版本核对','Pre-Norm 常写为 x+Attention(Norm(x))，Post-Norm 则写为 Norm(x+Attention(x))。排列改变梯度路径与训练稳定性，不能只靠页面方块数量说两种架构相同。原有单块演示是 Post-LN，新微型可训练解码器采用 Pre-RMSNorm，两个实验的约定在页内明示。'],
      ['嵌入权重共享是一种选择','输入嵌入表和输出词表头可以共享参数，在维度匹配时减少存储；也可以独立学习。SwiGLU 的三块矩阵、GQA 中减少的 K/V 投影都会改变参数账本。不要用“层数×宽度”代替完整计数；微型实验给出逐模块的参数量，资源估算页则显式采用总参数输入。']
    ],'RMSNorm(x)=x/√(mean(x²)+ε)<br>LN(x)=(x−mean(x))/√(var(x)+ε)<br>SwiGLU(x)=[SiLU(xWg) ⊙ (xWu)]Wd',
    '两项门控投影形状为 d×dff，向下投影为 dff×d；参数大约是 3ddff（此处不计偏置）。数值实验只展开门控核心，未把四个手动分量包装成训练过的层。',
    '写出 d=4、dff=8 时普通两层 FFN 与三矩阵 SwiGLU 的权重数。要保持参数量相近，SwiGLU 的 dff 应如何调整？',
    ['RMSNorm 必须把每行均值变为 0 吗？',['必须','不必，它不减去均值','只有向量含负数时必须'],1,'RMSNorm 按均方根缩放，重新中心化不是它的步骤。']);
  add('llm-cache','KV Cache、GQA 与 FlashAttention','生成一个新词，哪些计算可以复用？',
    '逐步增加序列长度，比较完整因果计算与缓存键值的结果；把算力、缓存容量和注意力矩阵分开。',
    '增加位置数，观察缓存计算与完整计算误差；再把 KV 头数从 8 改到 1，比较缓存估算。',
    ['attention','llm-rope'],['llm-cache','llm-gqa','llm-flash'],[
      ['Prefill 与 Decode 做的工作不同','Prefill 一次处理已知前缀，建立每层的历史 K/V；Decode 每步加入新词，只计算新位置的表示与新 K/V，再读取缓存。缓存保存的是模型内部激活，参数并没有更新。多层因果模型中历史表示不受未来新词影响，这使复用成为可能。'],
      ['缓存省重复投影，也要付出内存','标准 KV 缓存按层、请求、位置、KV 头和头维度增长。每步生成仍需让新查询和历史键计算匹配，并加权历史值；缓存没有让读取上下文的成本消失。本实验对人工 Q/K/V 逐行追加缓存，数值上核对完整因果注意力，不计整模型速度。'],
      ['GQA 减少的是键值头','MHA 通常每个查询头有对应的 K/V 头；MQA 所有查询头共享一组 K/V；GQA 处于两者之间，多个查询头分组共享。缓存量与 KV 头数相关，不能只用查询头数估算。分组共享会影响模型结构和质量，减小计算器中的头数不等于无代价转换已有权重。'],
      ['FlashAttention 不是删掉注意力连接','它通过分块和在线归一化等方式组织精确注意力计算，减少高带宽内存读写和中间矩阵显式存储。它与 KV cache 优化的环节不同，也不同于稀疏注意力或滑动窗口。精确算法的浮点累加顺序可能有差别，复杂度和真实加速取决于算子、形状与硬件。'],
      ['缓存复用必须匹配前缀与模型状态','跨请求的前缀缓存要对模型版本、提示词 token、位置及其他影响表示的配置进行匹配。量化缓存又会引入近似误差。分页管理解决分配和碎片问题，不直接改变模型学到的知识；部署时需要同时考虑最大并发、上下文长度及动态显存峰值。']
    ],'KV bytes=2×层数×batch×长度×KV头数×头维×每元素字节<br>缓存中 2 对应 K 和 V，不包括 Q',
    '当前估算假定每层同尺寸、全注意力、KV 为 16 位。滑动窗口、压缩潜变量缓存、不同层配置及内存对齐需要另设公式；不要把这里的估算直接用于所有架构。',
    '24 层、batch=1、2048 位置、8 KV 头、64 头维、2 字节时，先手算 KV 占用；改成 1 KV 头应恰为多少比例？',
    ['有了 KV cache，新查询还要读取历史键值吗？',['不用，所有注意力都省掉了','要，缓存省去的是可复用部分的重复计算','缓存会自动训练参数'],1,'历史 K/V 被保存，但新位置仍需与它们进行注意力计算。']);
  add('llm-train','训练一个微型解码器','从随机权重，训练到真的能续写。',
    '一个 260 参数、单头、单块的字符语言模型。完整训练嵌入、注意力与前馈，用真实采样生成短句。',
    '先生成一次，再训练 120 步，用相同前缀和采样种子再次生成。比较训练损失、验证损失和注意力，而不只挑好看的句子。',
    ['llm-objective','llm-block','backprop'],['llm-d2l','llm-pretrain','llm-rms'],[
      ['模型很小，但前向和反向是完整的','固定词表含 11 项，隐藏宽度 4，最大 8 个输入位置，单头因果注意力和宽度 8 的 ReLU 前馈。使用可学习绝对位置嵌入、Pre-RMSNorm 与独立词表头。所有 260 个参数进入自动微分与 Adam 更新，不是只训练末尾分类器，也不是播放预先保存的损失曲线。'],
      ['语料和划分公开在页面中','训练集是六句原创的颜色、动物与食物组合；验证集是两句未用于梯度的组合。任务让你检查因果预测的机械过程，而非获得通用语言能力。词表预先指定，验证集极小，反复查看后参与方案选择；低损失和看似通顺的输出都不能外推到真实语言任务。'],
      ['每一次更新做了什么','按固定顺序循环选一条训练句，构造 BOS 与 EOS、对齐错开一位的输入与标签，计算所有有效位置的平均交叉熵，再由标量计算带反向求梯度。全参数梯度按范数 1 裁剪，Adam 更新；没有 dropout 或权重衰减。这里 step 是一次句子更新，每六步才遍历训练句子一遍。'],
      ['损失显示用统一口径','显示的训练和验证损失在各自全部句子上按有效词元数加权，不把每句话的平均值等权相加。单步训练损失只对应当步句子，可能上下波动。表中参数族 RMS 展示数值尺度，不把权重大小当作特征因果重要性；当前前缀的注意力也只是一层的计算结果。'],
      ['生成会把自己选出的词接回去','输入前缀后读取末位置 logits，按温度和 top-p 抽样，再把新字符放回输入继续前向。BOS 是输入起始符，生成时不选它；EOS 会结束句子。最多使用八个输入位置，不做滑动窗口，也不复用 KV。生成可能出现错误、重复或提前停止；同模型同前缀同采样种子可重现。'],
      ['计算与保存都有明确上限','最多训练 600 次更新，每次仅一条短句，播放在每步之间让出主线程。页面隐藏或离开时停止；权重导出使用独立 visible-microgpt-v1 格式，包含 Adam 状态和更新计数。导入先验证候选再替换，失败保留原模型，不与原来的二维 MLP 模型文件混用。']
    ],'x=E[token]+P[position]<br>h=x+Attention(RMSNorm(x))<br>u=h+FFN(RMSNorm(h))<br>logits=Wout·RMSNorm(u)<br>L=mean(CE(logits, shifted labels))',
    '本模型矩阵按“输出×输入”存储，不用偏置。参数账本：词嵌入 44、位置 32、Q/K/V/O 共 64、三组 RMS 增益 12、前馈 64、词表头 44，合计 260。ReLU 的零点导数取 0；标量反向模式每步生成并释放运算带。',
    '前缀“蓝猫”没作为完整训练句出现过。固定采样种子对比训练前后生成，并从全部验证词元损失说明模型是否学到可组合规律；不要只展示一次成功续写。',
    ['这次哪些参数能被训练？',['只有词表输出头','嵌入、位置、注意力、归一化增益、前馈与输出头','只改变采样温度'],1,'所有 260 个模型参数参与梯度与更新，温度只用于生成。']);
  add('llm-budget','预训练预算与 Scaling','参数、数据和算力，要放在同一本账上。',
    '使用可审计的情景计算器，区分权重、优化器状态、激活、训练计算量和有效批量。',
    '固定模型参数，把训练 token 数翻倍；再把数据并行设备翻倍。检查哪些数字只是理想估算，哪些不会自动变小。',
    ['llm-train','descent'],['llm-scaling','llm-pretrain'],[
      ['预训练是优化问题，也是资源配置','模型参数更多通常需要更多数据和计算来训练充分。Scaling laws 是在特定数据、模型族和预算范围拟合的经验关系，不能把某个比例写成永远最优。还要考虑部署推理成本、质量目标和数据可获得性。此页只计算资源关系，不虚构一个能预测真实能力的万能曲线。'],
      ['6ND 是带假设的近似','对稠密 Transformer，常用训练 FLOPs≈6×参数量N×训练token数D 做数量级估算。它忽略部分注意力、嵌入、激活重算和其他开销，长上下文时可能偏离较多。页面把输入的“十亿参数”和“十亿token”换成绝对量后计算，不把它当作实际 GPU 利用率测量。'],
      ['显存远不止模型文件','权重的理论净载荷是参数数×位宽。常见混合精度 Adam 账本可含 2 字节低精度权重、2 字节梯度、4 字节主权重与 8 字节矩，共 16 字节/参数，但实现可能不同。激活、临时缓冲、通信和分配碎片还在此之外；能装下推理权重不代表能全参数训练。'],
      ['有效批量由多个层次相乘','若数据并行设备数为 Dp、每设备微批量 Bmicro、累积步数 A，则每次同步更新有效样本数为 Dp×Bmicro×A。变长文本更适合同时记录有效 token 数。梯度累积节省单个微批次激活内存，不消除参数与优化器状态；平均损失需要按有效样本或 token 数保持口径一致。'],
      ['并行训练有通信与流水空洞','数据并行复制模型并聚合梯度；张量并行切分某些矩阵；流水并行按层分段；优化器分片进一步减少重复状态。理想计算时间除以设备数没有反映通信、负载不均、数据加载和失败重启。计算器的利用率由你输入，输出为场景推算，不推荐购买任何特定硬件。']
    ],'C≈6ND<br>time≈C/(设备数×峰值 FLOP/s×利用率)<br>B_eff=B_micro×数据并行数×累积步数',
    '容量显示采用 GiB=2³⁰ 字节，参数 N 和数据 D 输入采用十进制十亿。训练状态预算未计激活；推理权重与 KV 的加和也只是下界。',
    '用 N=1B、D=20B 算 C，再用 100 TFLOP/s、40% 利用率、单设备算天数。写出会导致实际时间更长的三个因素。',
    ['模型文件 2 GiB，训练显存就只要 2 GiB 吗？',['是，参数量决定一切','不是，还需要梯度、优化器状态、激活等','只有使用 AdamW 才额外占用'],1,'训练保存的信息与纯推断不同，模型文件只是其中一项。']);
  add('llm-sft','指令微调与对话模板','让续写模型学会回答，监督在哪些位置？',
    '查看一条对话序列，切换全序列或仅回答损失；理解角色边界、EOS 与多轮训练。',
    '分别把问题和回答的正确词概率降低，观察 assistant-only 目标对哪一部分直接计分。',
    ['llm-objective','llm-train'],['llm-sft','llm-chat'],[
      ['SFT 改变学习数据与条件','监督微调从已有模型参数继续训练，在指令、上下文与示范回答上优化目标。数学上仍常用下一个词元交叉熵，但样本分布和损失位置改变。持续预训练更偏领域语料，SFT 更偏任务响应形式，二者可以组合。微调可能改善行为，也可能遗忘原有能力。'],
      ['对话消息最终仍是 token 序列','role 与 content 结构通常经 chat template 变成带边界标记的单序列。训练与推断必须使用匹配模板，避免重复 BOS/EOS 或遗漏 assistant 起始提示。role 名称不是在所有模型中都有相同编号的通用硬件指令；它的效果来自约定、训练与外部系统。'],
      ['只监督回答不等于不处理问题','assistant-only loss 屏蔽问题位置的直接预测损失，回答部分仍以问题为上下文。梯度可通过回答的计算传到问题的表示。多轮对话还需指定所有 assistant 轮次还是仅最后一轮计分，并处理工具响应。此例只显示一轮抽象序列和人工正确词概率，不实际微调语言模型。'],
      ['数据设计比堆数量更重要','示范应覆盖任务边界、输入变化、格式要求与信息不足时的行为。重复模板、低质合成回答和错误引用会被一起学入。划分验证数据时考虑同一问题改写与同一文档的关联，避免把几乎相同的模板随机分两边。先建立基线、再做小规模对照，比较任务正确率与原能力回退。'],
      ['停止词元与截断也在训练目标中','截断可能只保留问题却删掉回答，或把多轮边界截坏；全为屏蔽标签的样本应明确跳过而不是产生 NaN。EOS 不宜随意遗漏，它参与教模型何时停止。参数高效微调决定更新哪些权重，SFT 决定按什么数据与目标训练，两者是不同的分类轴。']
    ],'L_SFT=−Σ assistant_mask[t]·log p(yₜ | prefixₜ) / Σ assistant_mask[t]',
    '本例把角色标记视为抽象单个词元，并不使用任何提供商模板。权重冻结、LoRA 和全参更新都能与这种损失口径组合。',
    '给一段 user→assistant→user→assistant 对话写两种 loss mask：所有回答监督、仅最后回答监督。再标出哪些位置可被最后回答读取。',
    ['SFT 与 LoRA 是互斥的两种训练目标吗？',['是，只能选一个','不是，一个描述监督方式，一个描述参数更新方式','它们都是不改权重的提示词方法'],1,'SFT 可以用全参更新，也可以用 LoRA 等参数高效方式。']);
  add('llm-lora','LoRA 与 QLoRA','把一次权重更新，拆成两个瘦矩阵。',
    '真实训练一个 4×4 目标更新矩阵的低秩近似。看 A、B 怎样变化，以及秩为何限制表达。',
    '分别用 rank=1 与 rank=2 训练同一个目标，比较误差；观察 B 初始为 0 时，第一步 A 会不会变化。',
    ['llm-sft','vectors','backprop'],['llm-lora','llm-qlora'],[
      ['冻结基座，只学习增量','LoRA 写作 W′=W₀+(α/r)BA。基座 W₀ 固定，A 把输入投影到 r 维，B 再映射到输出空间。训练只优化低秩因子，而不是把原矩阵切出几行更新。推断时可以合并增量，但实际合并还要考虑量化与多适配器切换。'],
      ['低秩提供约束，也限制可表示更新','dout×din 的完整矩阵需要 dout·din 个参数，LoRA 因子需要 r(din+dout)。当 r 较小时通常更少；对小矩阵或 r 过大时不一定节省。本实验目标更新由两个独立的秩一块组成，因此 rank=1 存在结构性剩余误差，rank≥2 才具备精确表达的可能。'],
      ['为什么常令 B 为零、A 随机','零 B 保证初始化时增量为 0，初始模型等于基座。第一步 B 的梯度可非零，但 A 的梯度含 B，因此为 0；之后 B 变化，A 才开始学习。若 A、B 都为零，两者梯度都可能被堵住。实验真实计算矩阵 MSE 梯度，但不是对语言任务进行 LoRA 微调。'],
      ['α、r 与插入位置都需说明','经典缩放为 α/r，其他变体会改变缩放。把适配器放在 Q/V、所有注意力投影或前馈模块，会改变参数量、效果和预算；仅说“用了 LoRA”不足以复现。这里固定 α=r，使缩放为 1，比较秩而不把缩放效应混进结果。'],
      ['QLoRA 冻结量化基座，计算仍需合适精度','QLoRA 结合低位基座表示与高精度适配器更新，涉及 NF4、双重量化、分页优化器等设计。它不是把所有梯度都变成四位，也不意味着显存仅等于四位模型文件。量化基座、激活、梯度与优化器分别记账；本章不实现 NF4，后面的均匀量化实验也不能替代它。']
    ],'ΔW=(α/r)BA<br>A∈Rʳˣᵈⁱⁿ, B∈Rᵈᵒᵘᵗˣʳ<br>参数数=r(din+dout)<br>L=mean((BA−target)²)',
    '本例固定 W₀=0 只拟合增量，α/r=1。目标为 4×4 对称秩二矩阵，SGD 学习率 0.5；每次更新先同时计算 A/B 梯度，再更新，最多 300 步。',
    '令 din=dout=4096、r=8，算全矩阵与适配器参数数；再解释“可训练参数少了”为什么不等于模型权重和前向激活都消失。',
    ['初始 B=0、A 随机时，第一步 A 的梯度是什么？',['一般非零','为零，之后 B 更新才可能传回来','等于所有输入的均值'],1,'∂L/∂A 含有 B，零 B 使第一步的这项梯度为零。']);
  add('llm-align','RLHF、DPO 与推理训练','偏好信号，怎样改变回答概率？',
    '调整偏好对的对数概率差、参考策略和 β，直接查看 DPO 损失与局部梯度。',
    '保持策略概率差不变，只改变参考差；观察“当前更喜欢 chosen”与“相对参考提高了偏好”是否相同。',
    ['llm-sft','loss'],['llm-dpo','llm-rlhf','llm-grpo'],[
      ['示范、偏好和可验证奖励不同','SFT 提供一条希望模仿的回答，偏好数据提供同一提示下 chosen 与 rejected 的比较；可验证奖励则可能依据测试通过、数学答案或规则打分。奖励和偏好反映任务及标注约定，存在噪声、偏见和投机空间。优化奖励提升不自动等于所有质量维度提升。'],
      ['RLHF 通常把奖励模型与策略更新分开','一条经典路线是示范微调、训练偏好奖励模型，再用强化学习更新策略并约束对参考策略的偏离。PPO 用重要性比率和裁剪等机制控制更新，需要 rollout 与优势估计等环节。RLHF 是更广的训练范式，不能把所有偏好学习都归成同一个具体算法。'],
      ['DPO 使用相对参考策略的序列概率比','令策略的 chosen/rejected 对数概率差为 a，参考模型的差为 b，DPO 的单对损失为 −log sigmoid(β(a−b))。序列概率通常累加回答 token 的 log probability；不是把每步 Softmax 随手平均。实验只独立调这个标量目标，没有生成偏好数据或运行策略训练。'],
      ['局部梯度不等于真实训练全貌','对 a 求导为 −β sigmoid(−β(a−b))，方向推动相对优势增大。实际模型一个参数会影响许多样本及两个回答的概率，可能通过降低 rejected 概率拉开差距；不能从这个一维图断言 chosen 绝对概率必然上升。β 的作用也要结合目标推导与训练设置理解。'],
      ['推理训练和推理时计算是两条轴','组相对方法可以用同提示多个候选的奖励比较构造优势，GRPO 是一类实例；奖励为零或组内相同会影响有效训练信号。推理时则可以增加采样、验证、搜索或反思预算。更长的输出本身不等于更好推理，需要在正确性、成本和作弊风险上测量。蒸馏还可把教师输出或分布用于学生训练，但质量受教师与筛选约束。']
    ],'a=log πθ(y+|x)−log πθ(y−|x)<br>b=log πref(y+|x)−log πref(y−|x)<br>L=softplus(−β(a−b))<br>∂L/∂a=−β·sigmoid(−β(a−b))',
    '这是标准 sigmoid DPO 单对损失；没有实现 PPO/GRPO 的完整训练器。界面的“偏好概率”是该偏好模型的预测，不是回答真实正确概率。',
    '当 a=2、b=3 时模型已经更偏好 chosen，DPO 相对优势却为负。解释为什么，然后求 β=0.5 时的损失和梯度方向。',
    ['a>0 能保证 a−b>0 吗？',['能','不能，还要减去参考模型的偏好差','取决于回答字数是否相同'],1,'DPO 比较相对参考变化，当前绝对偏好方向不足以确定相对优势。']);
  add('llm-decode','解码与采样策略','同一组 logits，可以生成不同的行为。',
    '把温度、top-k、top-p 的处理顺序展开，真正抽样并比较理论概率与经验频率。',
    '先令 k=1，再改变温度和 top-p；然后恢复全部候选，抽样 200 次，比较频率波动。',
    ['tokens','llm-objective'],['llm-bpe','llm-d2l'],[
      ['解码规则不修改权重','模型产生 logits，解码器据此挑选词元。贪心取最高分；采样保留随机性；束搜索同时维护多个候选前缀。序列整体概率是条件概率乘积，逐步贪心不保证全局最优；束宽和长度惩罚改变搜索行为，不是无成本提高事实正确性的按钮。'],
      ['温度与候选截断解决不同问题','温度除 logits 调整集中程度，top-k 按数量限制候选，top-p 按累计概率限制。操作顺序会影响保留集合。本实验明确采用温度→top-k→重归一化→top-p→重归一化。极小温度有数值风险，界面下限为 0.05；贪心通过 k=1 表达，不直接除以零。'],
      ['经验频率有抽样波动','理论分布是当前设置下的精确概率，200 次样本频率只是一次有限实验。某个小概率候选未出现不表示概率为零。配置改变时计数归零，防止把不同分布的抽样混成一张图。种子固定，让相同操作顺序可复现。'],
      ['停止、重复与约束生成','EOS、最大长度、停止字符串或结构化格式约束可以终止或限制生成。重复惩罚改变后续分数，应说明究竟按出现频次还是是否出现处理。JSON 语法有效不等于字段事实正确，工具调用通过 schema 校验也不等于参数适合真实执行；仍需外部验证和权限管理。'],
      ['本实验与真正自回归生成分开','这里从人工 logits 的固定分布抽样，显示概率性质，不会把候选接回上下文。第 23 章的微型解码器才会每次重新前向并续写。真实任务还可结合验证器、重排序与多候选预算，要同时测正确率、总 token 成本和延迟，而不只看单次回答。']
    ],'pᵢ=softmax(zᵢ/T)<br>S=top-k → 累计概率达到 top-p 的最小前缀<br>H(p)=−Σ pᵢ log pᵢ',
    '这里按概率降序，平分时以原词表顺序打破平局。EOS 在本固定分布实验只是一个可抽中的候选；计数演示不因抽到它停止整个统计试验。',
    '概率为 [0.5,0.3,0.2]，k=2、p=0.7。按本页顺序，应在 top-k 后先把前两项归一化，再判断 nucleus 包含几项。',
    ['k=1 时，正温度变化会产生更多可选词元吗？',['会','不会，候选集合只有一项','只有温度小于 1 才会'],1,'温度不改变有限 logits 的最大项，截断后只剩一个候选。']);
  add('llm-quant','量化与精度预算','把连续权重，放进有限的整数格。',
    '比较位宽、分组缩放与离群值裁剪，展示整数编码、重建值、误差和元数据开销。',
    '把离群值由 1 增大到 12，观察其他小权重的重建；改成更小分组，看尺度开销与误差怎样权衡。',
    ['vectors','llm-budget'],['llm-quant','llm-qlora'],[
      ['量化是数值表示与计算策略','把浮点值映射到少量离散编码，再通过 scale、zero point 等重建。它可以作用于权重、激活或 KV cache，各自对质量和性能影响不同。低位模型通常还需要在适当精度中累加或反量化。位数变小只给出潜在存储优势，实际速度取决于硬件与算子支持。'],
      ['本页实现对称均匀量化','每组 scale=max(abs(x))/qmax，q=clamp(round(x/scale))，再用 q×scale 重建。qmax=2^(bits−1)−1，故有一个有符号编码没有使用。全部为零时 scale 取 1 以避免除零。可显式固定裁剪边界，误差始终对原始未裁剪值计算。'],
      ['离群值会抢走动态范围','当一组中有一个极大权重，scale 由它决定，许多小权重会量化为相同整数甚至 0。更小分组能减少互相牵连，但需要更多 scale 元数据。裁剪牺牲离群值精度换取其他点的分辨率，不能只看较漂亮的主体曲线而忽略被裁掉的误差。'],
      ['压缩率不能只写 32 除以 4','小组还要保存缩放参数，可能有零点、索引、对齐和打包开销。本实验给出理想 payload bits=数值个数×位宽+分组数×32，尚未包括容器与对齐。因此极小数组、逐元素尺度时，低位甚至可能不省空间。真实测量要看最终文件和运行时分配。'],
      ['NF4、GPTQ 与训练量化不是同一件事','NF4 使用不同于均匀整数的数值方案，GPTQ/AWQ 等权重量化方法依据不同校准或误差补偿策略；QAT 在训练中显式考虑量化，PTQ 在训练后处理。这里没有实现这些方法，不把小向量 MSE 当作语言质量测试。压缩后需重新测任务、极端输入和吞吐。']
    ],'s=max(abs(x))/qmax<br>q=clip(round(x/s),−qmax,qmax)<br>x̂=q·s<br>MSE=mean((x−x̂)²)',
    '编码显示的是普通 JavaScript 整数，浏览器实际没有把数组压成 4bit 位流；容量栏是给定存储协议下的理论账本，计算时间也不是 GPU 量化内核基准。',
    '16 个值、4bit、每 4 值共享一个 32bit scale，算理论净载荷。再比较逐值一个 scale，解释元数据为何主导小样本。',
    ['低位 MSE 更低能证明所有语言任务都更好吗？',['能','不能，还需在真实模型和任务上评估','只要使用 8bit 就可以'],1,'局部权重误差与模型任务表现并非简单一一对应。']);
  add('llm-moe','MoE 与稀疏路由','参数很多，但每个词元只走几条路。',
    '给六个词元分配四个专家，调整路由偏置、top-k 与容量，观察负载及溢出。',
    '提高专家 1 的分数，让所有词元偏向它，再降低容量。查看被丢弃的是词元还是一条专家分支。',
    ['llm-block','llm-decode'],['llm-moe'],[
      ['专家通常是可学习的子网络','稀疏 MoE 用路由器为每个 token 选择少数专家，专家可以替换某些前馈层。它与聊天中的多个角色或多智能体分工不同；这里的专家是模型内部数值模块。总参数量、每 token 激活参数量和真正吞吐都应分别报告，未激活的专家权重仍要存储或加载。'],
      ['分数、选择和加权是三个步骤','路由 logits 经 Softmax 形成概率，选 top-k 专家，再按约定合并其输出。本实验有四个专家、六个 token，展示候选和选择权重，但没有训练专家或执行专家网络。每项分支权重先在 top-k 内归一化，容量限制随后处理。'],
      ['容量不足需要明确策略','本实验按 token 顺序接收每个专家分支，满容量时丢弃后来的分支，且不对幸存分支重新归一化。这样能看到负载压力，但它只是一个教学约定；实际系统可选重路由、溢出处理或 dropless 方案。一个 token 的一条分支被拒绝，不必意味着整个 token 完全消失。'],
      ['负载均衡兼顾统计与系统','若少数专家被过度使用，其他专家训练不足，热门设备成为瓶颈。辅助均衡损失、路由噪声或无辅助损失方案各有取舍。每个专家收到的 token 数量、分支权重和是否丢弃，应分别观测。分组大小与设备通信也会改变有效速度。'],
      ['稀疏激活不等于免费扩大模型','专家并行会引入 token 分发与聚合通信，batch 较小时利用率可能较低。推理还受权重带宽、路由不均与跨设备传输限制。仅看到“总参数大、激活参数小”，不能推出一张小显卡就能装下或高效运行；要结合部署拓扑与总权重容量。']
    ],'p=softmax(router(x))<br>y=Σ e∈topk p̃e·Expert_e(x)',
    '本页只显示路由、容量和权重；输出向量依赖未在本页实现的专家网络。容量按“每专家最多接受多少 token 分支”计算，不是整个序列长度。',
    '六个 token、top-2 共请求 12 条分支，四专家容量各 2，总容量 8。即使负载理想均衡，至少有几条分支无法接受？',
    ['MoE 的“专家”一定是一个能独立聊天的智能体吗？',['是','不是，通常是模型内部的参数子网络','只要专家数多于 2 就是'],1,'模型结构中的专家与应用层多代理不是同一概念。']);
  add('llm-rag','RAG 与外部知识','把证据送进上下文，而不是写进权重。',
    '在小型中文资料库中真正检索、排名和选择证据，展示没有检索到与检索错误时的区别。',
    '搜索“权重会更新吗”，比较不同证据的得分；换一个资料库没有的问题，检查是否应拒绝强行拼出答案。',
    ['llm-sft','llm-cache'],['llm-rag','llm-eval'],[
      ['RAG 把信息检索接到生成前面','典型流程是文档解析、切块与索引，查询改写或编码，召回、重排序、证据组装，再交给生成模型。检索阶段增加可更新的外部信息，不直接修改模型参数；SFT 可以教引用方式，LoRA 可以适配行为，RAG 提供资料，它们可以组合。'],
      ['本例检索是真的，生成环节没有伪装','使用字符与相邻二元片段 TF-IDF 向量，再以余弦相似度排名六条原创资料；不是语义嵌入模型或真实向量数据库。你能看清得分和证据原文，页面不调用 LLM 生成答案。没有匹配信号时得分为 0，应显示无有效证据，而非把第一条当作答案。'],
      ['切块和召回决定能否找到完整证据','块太短可能丢上下文，太长可能稀释主题并消耗窗口。重叠、标题层级、表格、引用和元数据都有意义。稀疏检索善于明确词项匹配，向量召回提供另一类相似信号，重排序可再评估查询与候选关系；增加 top-k 会带来噪声与上下文成本。'],
      ['证据存在与答案忠实要分别评价','检索 Recall@k 关心相关文档是否召回，生成忠实度关心回答是否被证据支持。高相似度不是文献质量或真实性背书；引用编号也可能指向并未支持结论的段落。应保留来源、版本与定位，使用人工抽检或明确规则核实关键事实。'],
      ['外部材料既是证据，也是不可信输入','文档中的指令、网页提示和工具返回应作为待分析内容，不能自动提升为系统权限。检索结果可能含过期数据或注入性文字；应用需隔离指令、限制工具权限、核验输出，敏感资料还要控制索引与检索授权。增加上下文无法替代这些边界。']
    ],'tfidf(w,d)=count(w,d)·[log((1+N)/(1+df(w)))+1]<br>score=dot(q,d)/(norm(q)·norm(d))',
    '分词规则是本页自定义的字符加二元片段，得分仅表示词项匹配。文档内容全部本地，查询不外发；预设资料不包含你的个人文件。',
    '设想给资料库增加一段相同关键词但结论相反的文本。解释检索器为何可能高分召回它，以及生成前还需要哪些证据筛选。',
    ['RAG 召回一个段落，就能保证回答中的每个说法都正确吗？',['可以，检索分数已经证明真实性','不能，要分别检查来源可靠性、证据支持和生成忠实度','只有 top-k=1 可以'],1,'检索相似、证据有效、回答正确是需要分别验证的环节。']);
  add('llm-eval','评估、污染与不确定性','一个总分，藏得下多少种错误？',
    '调节两个任务切片的正确数与业务权重，比较固定样本配比下的微平均、宏平均与区间。',
    '常见任务固定 90 个样本、罕见任务固定 10 个。改变正确数和业务权重，观察总体分数怎样掩盖少数任务的失败。',
    ['metrics','generalization','llm-rag'],['llm-eval','validation'],[
      ['先定义评价对象和判定规则','评价基座语言模型、对话助手、检索应用和工具代理，需要不同任务与证据。困惑度、精确匹配、单元测试、引用支持、人类偏好、延迟和成本各有盲区。先定义任务集合、输入版本、解码设置与成功规则，再报告分数；不能随意挑最有利指标。'],
      ['样本加权与任务等权会得出不同结论','微平均按每条样本计数，宏平均先算每个切片分数再等权，业务权重则显式给不同任务重要性。本实验用你输入的二项正确/错误计数直接计算，不声称是某个模型的基准成绩。少数困难任务可能在总体高分中被掩盖。'],
      ['区间依赖统计假设','页面用 Wilson 95% 区间描述合并二项正确率，假定样本近似独立且具有共同抽样含义。真实题目可能共享文档、模板或来源，切片难度也不同，此时简单合并区间不代表全部不确定性。模型比较更适合配对设计，按问题或文档分组重采样。'],
      ['污染、提示词和裁判偏差一起审计','训练集中出现基准题或改写题，会削弱测试独立性。提示词版本、少样本示例和答案解析也可显著改变结果。LLM-as-judge 需要处理位置偏好、长度偏好、同源偏好与可重复性，保留人工核验；评分器给出漂亮文字不等于判定可靠。'],
      ['研究结果要能追溯到样本','保留数据快照、模型或接口版本、采样配置、失败日志与错例类别，报告不适用范围。对 RAG 分别测检索与生成，对代理分别测规划、工具正确性与任务完成。每次迭代用固定回归集并保留新发现的失败，观察质量—成本—时延的联合变化。']
    ],'micro=(c₁+c₂)/(n₁+n₂)<br>macro=(c₁/n₁+c₂/n₂)/2<br>业务分数=w·c₁/n₁+(1−w)·c₂/n₂',
    'Wilson 区间不是“模型真实能力有 95% 概率落在里面”的万能声明；它来自特定二项抽样模型。页面没有用区间为任意分布偏移或主观评分背书。',
    '常见任务 81/90，罕见任务 2/10，手算 micro 与 macro。把两种任务权重改成各半，讨论哪一个更符合部署目标。',
    ['总体准确率很高，可以不看少数任务切片吗？',['可以','不可以，样本配比可能掩盖关键失败','只要样本总数超过 100 就可以'],1,'评价权重与实际需求不一定相同，切片让风险可见。']);
  add('llm-multimodal','多模态与视觉词元','图像怎样进入语言模型的序列？',
    '把一张 8×8 小图切成 patch，经固定线性投影变成向量，计算视觉序列长度的变化。',
    '在 2×2 与 4×4 patch 之间切换，比较位置数量、每块维度和全部 patch 是否覆盖原图。',
    ['convolution','transformer'],['llm-vit','llm-pretrain'],[
      ['不同模态先转成可处理表示','文本先分词，图像可以切 patch、经视觉编码器获得特征，音频可以从帧或编码器输出进入模型。随后用投影、跨注意力或其他连接方式与语言表示对接。所有东西变成向量不等于所有任务结构相同；模态的时空关系、精度和对齐目标仍需保留。'],
      ['Patch 与文本词元有相似的接口','本例把单通道 8×8 图像按不重叠方块切分，按行展平，每个 patch 乘固定矩阵映射为四维。加位置后可以输入 Transformer，但本页只演示切块与投影，不训练视觉编码器或语言模型。四维数值来自随机教学权重，没有“看懂图片”。'],
      ['分辨率、patch 大小与序列成本','固定分辨率下更小 patch 意味更多视觉位置。标准全注意力的权重格数随总序列长平方增长；有的视觉语言模型采用压缩、重采样、裁剪或跨注意力，费用不一定按这个简单公式计算。界面标明它只估算一个朴素拼接全注意力场景。'],
      ['对齐可以发生在不同层','CLIP 类对比学习对齐图文表示，视觉语言生成模型还要把图像证据关联到输出文本。图像编码器冻结或训练、连接器结构与指令微调数据都影响能力。OCR、空间关系、计数和细粒度图表理解是不同评价维度，不能用描述流畅替代准确读图。'],
      ['多模态仍有证据与安全边界','图像可能包含文字指令、错误坐标或被裁掉的关键信息。模型需要把图中内容与系统权限分开，应用还要保护隐私、核验关键数值。长音视频进一步增加时间位置和采样取舍；本站不加载真实图片、音频或多模态权重，保持演示可离线。']
    ],'patch_count=(H/P)×(W/P)<br>patch_flat=P²×channels<br>embedding=patch_flat_vector × Wproj',
    '这里只有灰度 C=1，H=W=8，P∈{2,4}，投影宽度 4。未加入 CLS token；整个 patch 数量与每块的输入维度分别显示。',
    '把 224×224 RGB 图像按 16×16 patch 切分，算位置数量、单个 patch 展平长度；再说明加入 100 个文本词元后的朴素注意力格数。',
    ['更小 patch 必然减少序列长度吗？',['是','相反，在固定分辨率下通常增加位置数','与图像尺寸无关'],1,'每边块数为 H/P 与 W/P，减小 P 会增加 patch 数。']);
  add('llm-serving','部署、批处理与学习路线','把模型变成服务，需要管理哪些等待？',
    '用离散调度模拟比较静态批处理与连续批处理，再回到训练、缓存和应用评估的全流程。',
    '把一个请求的输出拉长，比较两种调度下其他短请求的完成时刻与空闲槽位。',
    ['llm-cache','llm-budget','llm-eval'],['llm-serving','llm-cache','llm-book'],[
      ['吞吐与单请求延迟不是同一个目标','TTFT 描述首词等待，TPOT 描述后续每词延迟，端到端延迟还含排队、输入预处理和全部生成。一次并行处理更多请求可能提高吞吐，但也影响显存与等待。系统评估应固定输入输出长度分布及并发策略，不把不同任务的 token/s 直接横比。'],
      ['静态批处理为何留下空槽','静态批次等整批请求结束再引入下一批，短请求早结束后槽位可能空着。本页假设所有请求 t=0 到达，每个 tick 每个活跃请求恰好生成一个 token，显示这种浪费。没有计算 prefill、KV容量、通信与真实GPU核，因此 tick 是教学时间单位，不是毫秒。'],
      ['连续批处理在释放的槽位补入请求','请求完成后下一 tick 用新请求填空，可提高本简化场景的占用率。但真实系统还要调度新前缀计算与已有解码，可能需要分块 prefill、公平性和内存预算。PagedAttention 处理 KV 分配与共享等问题，不能单凭图中填满槽位推断实际加速倍数。'],
      ['部署还包括版本与权限','模型、适配器、tokenizer 和 chat template 的版本要匹配，资源限制与超时要可回收。工具调用属于外部执行，不是模型文字输出自动获得执行权；应用应核验 schema、权限、用户确认和结果。保留回归评估与成本记录，区分模型缺陷、提示词问题和系统错误。'],
      ['下一步回到可运行教材','本路线建立了词元—表示—损失—训练—适配—生成—应用的主线。进一步可用 D2L 的序列模型、注意力、优化和计算性能章节学习 PyTorch，再把本页手算映射到张量代码。扩展方向包括完整分布式训练、蒸馏与验证器、长上下文、多模态和安全评估；本站没有用浏览器模拟工业集群。']
    ],'槽位利用率=Σ请求生成长度 / (总tick×槽位数)<br>平均完成时间=Σ完成tick / 请求数',
    '完成时间从共同到达时刻 0 计起；静态批内已完成请求立即记完成，但其空槽要到整批结束才复用。此模拟不产生模型输出，也不计首词延迟。',
    '请求长度为 [2,8,2,8]，两个槽位。手画两种调度甘特图，比较总时间与平均完成时间；再说明加上 prefill 后还缺哪些假设。',
    ['模拟中槽位利用率翻倍，真实服务一定快两倍吗？',['一定','不一定，真实模型计算与系统开销未包含','只要用 GPU 就一定'],1,'教学调度模型展示等待机制，不是硬件基准。']);

  C.chapters.forEach((c,i)=>{c.no=String(i+1).padStart(2,'0');});
  C.llmChapters = C.chapters.slice(start).map(c=>c.id);
  const terms = [
    ['自回归分解','Autoregressive factorization','把序列联合概率写成按位置条件概率的乘积。','llm-objective','读取“我爱”后预测下一词，整句对数概率是各步 log probability 之和。'],
    ['输入与标签移位','Shifted labels','输入位置 t 用来预测序列位置 t+1 的监督排列。','llm-objective','输入 BOS、我、爱；标签我、爱、猫。避免模型与数据管线重复右移。'],
    ['损失掩码','Loss mask','决定哪些位置的损失被纳入训练目标及分母。','llm-sft','问题不计直接损失，回答仍可以读取问题。'],
    ['注意力掩码','Attention mask','决定某个查询可读取哪些键的位置约束。','llm-objective','因果掩码允许 j≤i，文档隔离还需 doc(i)=doc(j)。'],
    ['语料混合','Data mixture','训练中来自不同语言、领域与来源的数据比例。','llm-data','提高代码样本权重会改变训练分布，不能只按总 token 解释结果。'],
    ['数据打包','Sequence packing','将多个短样本按约定组织到固定长度窗口以减少空位。','llm-data','EOS 标记文档边界，但未自动形成硬注意力隔离。'],
    ['近重复检测','Near deduplication','识别并处理高度相似而非字节完全相同的数据。','llm-data','网页正文只改一行日期，精确哈希可能不同，内容仍高度重复。'],
    ['RoPE','Rotary position embedding','按位置对 Q/K 的二维子空间做旋转。','llm-rope','同频 q′ᵀk′只依赖两位置的差，而不是共同平移量。'],
    ['RMSNorm','Root mean square normalization','以均方根缩放特征，通常不减去均值。','llm-block','全为正数的向量经 RMSNorm 后不必均值为 0。'],
    ['SiLU','Sigmoid linear unit / Swish','激活 x·sigmoid(x)，负区间可非零。','llm-block','SiLU(−1)约为−0.269，并不等于 ReLU 的 0。'],
    ['SwiGLU','Swish gated linear unit','SiLU 门控分支与另一投影逐元素相乘再向下投影。','llm-block','两个 d→dff 分支加一个 dff→d 分支。'],
    ['权重绑定','Weight tying','不同组件共享同一个参数张量。','llm-block','词嵌入与输出头可共享参数；本微型解码器没有绑定它们。'],
    ['预填充','Prefill','一次处理已知提示词并建立相应表示或 KV 缓存。','llm-cache','长提示词的首词等待可能主要发生在 prefill。'],
    ['解码阶段','Decode phase','生成期间逐步加入新词元的计算阶段。','llm-cache','读取缓存为新位置算注意力，再选择下一个 token。'],
    ['KV 缓存','KV cache','保留过去位置在各层的键和值以供后续读取。','llm-cache','不会存储问题的一个自然语言摘要，也不会自动训练权重。'],
    ['GQA','Grouped-query attention','多个查询头分组共享较少的 K/V 头。','llm-cache','8 个查询头配 2 个 KV 头，每组共享的查询数为 4。'],
    ['MQA','Multi-query attention','所有查询头共享一组键和值头的设计。','llm-cache','缓存量按 1 个 KV 头而不是查询头总数计算。'],
    ['FlashAttention','IO-aware exact attention','通过计算组织和分块减少中间存储与内存读写的精确注意力算法。','llm-cache','它没有把精确 softmax 注意力替换成固定稀疏图。'],
    ['计算带','Autodiff tape','记录前向运算依赖和局部导数，供反向模式累积梯度。','llm-train','本微型解码器每步重新生成短计算带，反向结束后释放。'],
    ['基座模型','Base model','通常指经过通用预训练、尚未特定行为适配的模型。','llm-sft','能续写文本与能按对话规范回答问题是不同训练目标。'],
    ['预训练','Pretraining','在大范围数据及目标上学习可后续复用的参数。','llm-budget','下一个词元预测是一类预训练目标，不是唯一目标。'],
    ['持续预训练','Continued pretraining','从已有参数继续在新语料上进行预训练型学习。','llm-sft','领域原文训练与示范问答 SFT 的数据结构不同。'],
    ['SFT','Supervised fine-tuning','在监督任务或示范响应上继续优化模型。','llm-sft','可以只对 assistant 回答位置计入交叉熵。'],
    ['对话模板','Chat template','把角色消息转成模型期望的单个词元序列的规则。','llm-sft','训练与推断的 assistant 起始标记需要匹配。'],
    ['PEFT','Parameter-efficient fine-tuning','只更新部分或新增少量参数的适配方法族。','llm-lora','LoRA 是一种 PEFT，二者不是所有场景下的同义词。'],
    ['LoRA','Low-rank adaptation','冻结基座并用两个低秩因子表示权重更新。','llm-lora','ΔW=(α/r)BA，参数数为 r(din+dout)。'],
    ['QLoRA','Quantized low-rank adaptation','在冻结低位基座上训练适配器的一类微调设计。','llm-lora','量化的是基座表示，不能由此推断所有梯度都为 4bit。'],
    ['适配器','Adapter','用于增加或改变特定任务行为的可学习附加参数组件。','llm-lora','更换 LoRA 文件应同时核对基座版本和插入模块。'],
    ['RLHF','Reinforcement learning from human feedback','利用人类反馈形成奖励或偏好信号来调整策略的范式。','llm-align','示范、奖励建模与策略优化承担不同职责。'],
    ['奖励模型','Reward model','把输入与响应映射为任务质量或偏好分数的模型。','llm-align','奖励分数可能带偏差，不是事实正确性的绝对尺度。'],
    ['DPO','Direct preference optimization','通过相对参考策略的回答概率比直接优化偏好。','llm-align','目标取决于 a−b，而不只是 chosen 概率高于 rejected。'],
    ['PPO','Proximal policy optimization','利用重要性比率和裁剪等方法控制策略更新的一类算法。','llm-align','在 RLHF 中常需 rollout、奖励与优势估计；本站仅文字讲解。'],
    ['GRPO','Group relative policy optimization','以同提示的一组候选相对奖励构造训练信号的策略优化方法。','llm-align','组内奖励全相同时有效比较信号可能不足。'],
    ['优势估计','Advantage estimation','衡量动作或响应相对基准表现的训练信号。','llm-align','奖励减基线可减少部分方差，但基线定义需与算法对应。'],
    ['奖励投机','Reward hacking','策略利用评分机制漏洞而未实现真实目标的现象。','llm-align','通过格式线索获取高分却答错内容，需要独立检查。'],
    ['蒸馏','Knowledge distillation','用教师模型的输出、分布或其他信号训练学生。','llm-align','教师错误和数据筛选偏好也可能传给学生。'],
    ['推理时计算','Test-time compute','在模型使用阶段增加采样、搜索、验证等计算预算。','llm-decode','多候选加测试验证与单纯写更长文本并不相同。'],
    ['束搜索','Beam search','同时保留多个高分候选前缀的序列搜索。','llm-decode','逐步贪心不保证整个序列的最高概率路径。'],
    ['量化','Quantization','把数值映射为较少离散编码并按规则重建。','llm-quant','4bit 编码还需要 scale 等元数据。'],
    ['量化尺度','Quantization scale','连接整数编码与实数重建值的缩放因子。','llm-quant','s 很大时多个小权重可能映射到同一个整数。'],
    ['离群值','Outlier','在某个数值分布中幅度或位置显著偏离主体的值。','llm-quant','一组中的大权重可能主导量化 scale。'],
    ['PTQ','Post-training quantization','在模型训练后进行量化处理。','llm-quant','通常还需校准与任务回归，不能只看位宽。'],
    ['QAT','Quantization-aware training','训练时显式考虑量化影响的设计。','llm-quant','目标是适应离散表示，并不等于普通浮点训练后改存储类型。'],
    ['混合专家','Mixture of experts / MoE','由路由器分配输入到多个参数专家的模型结构。','llm-moe','稀疏激活减少部分每 token 计算，总权重仍需存储。'],
    ['路由器','Router','为词元或输入选择专家并计算分配权重的组件。','llm-moe','高分集中在一个专家时，容量和负载可能成为瓶颈。'],
    ['专家容量','Expert capacity','一次路由中每个专家允许接收的 token 分支预算。','llm-moe','满容量后的分支可丢弃、重路由或另行处理，要声明规则。'],
    ['RAG','Retrieval-augmented generation','以外部检索证据条件化生成的系统或建模方式。','llm-rag','检索得到资料不直接改写模型权重。'],
    ['重排序','Reranking','在初步召回后用更细的相关性判定重新排列候选。','llm-rag','高召回的候选里可能存在词相似但不支持问题的段落。'],
    ['证据忠实度','Faithfulness / Groundedness','回答是否受到给定证据的支持。','llm-rag','引用了文献不等于文献确实支持那句结论。'],
    ['基准污染','Benchmark contamination','训练或选择过程接触了基准内容，损害评价独立性。','llm-eval','原题改写或答案泄漏也应纳入排查。'],
    ['模型裁判','LLM-as-a-judge','由语言模型对其他输出进行评分或比较的方法。','llm-eval','需要考虑位置偏好、长度偏好与人工抽检。'],
    ['宏平均','Macro average','先分别算各切片指标再按切片等权平均。','llm-eval','90 个简单题和 10 个困难题的切片各占一半。'],
    ['微平均','Micro average','先汇总样本级计数再计算总体指标。','llm-eval','总体准确率按每个样本而不是每个任务等权。'],
    ['视觉词元','Visual token / Patch token','由图像局部块或视觉编码器产生的序列表示。','llm-multimodal','本页 8×8 图、2×2 patch 形成 16 个位置。'],
    ['多模态对齐','Multimodal alignment','让不同模态表示或输出在任务语义上形成对应。','llm-multimodal','图像描述流畅不保证计数或图表数值读对。'],
    ['TTFT','Time to first token','请求开始到收到第一个输出词元的时延指标。','llm-serving','可能包含排队、预处理与 prefill。'],
    ['TPOT','Time per output token','解码期间平均每个后续词元的时间指标。','llm-serving','计算口径需说明是否排除首词。'],
    ['连续批处理','Continuous batching','在请求完成后动态引入新请求的批次调度。','llm-serving','补空槽提高潜在利用率，但真实系统还受 prefill 与 KV 预算约束。'],
    ['MFU','Model FLOPs utilization','有效模型计算量与硬件理论峰值能力的相对利用指标。','llm-budget','计算器把利用率作为假设输入，不是在测量硬件。'],
    ['激活重计算','Activation checkpointing','少保存部分中间激活，在反向时重新计算以换取内存。','llm-budget','降低激活内存通常会增加计算量，参数状态不会因此消失。'],
    ['数据并行','Data parallelism','多设备处理不同数据并聚合训练更新的策略。','llm-budget','模型复制、梯度通信与有效批量都要计算。'],
    ['张量并行','Tensor parallelism','把部分张量运算或权重分布到不同设备上协作计算。','llm-budget','切分矩阵需要相应通信，不是独立训练几个模型。'],
    ['流水并行','Pipeline parallelism','按层或阶段分设备，利用微批次推进流水。','llm-budget','流水空洞与阶段负载不均会降低实际利用率。']
  ];
  const known = new Set(C.glossary.map(t=>t.term));
  for (const [term,en,definition,chapter,example] of terms) {
    if (known.has(term)) throw new Error('重复词条：'+term); known.add(term);
    C.glossary.push({term,en,definition,chapter,example,refs:C.chapters.find(c=>c.id===chapter).refs.slice(0,2),related:[]});
  }
  for (const t of C.glossary.filter(t=>C.llmChapters.includes(t.chapter))) t.related=C.glossary.filter(p=>p.chapter===t.chapter&&p.term!==t.term).slice(0,4).map(p=>p.term);
  C.llmReading = [
    ['先补数学和优化','向量、自动微分、Softmax、MLP、梯度与稳定性','https://zh.d2l.ai/chapter_preliminaries/index.html','llm-objective'],
    ['序列与语言建模','从 RNN 的语言模型目标理解到 Transformer 的并行计算','https://d2l.ai/chapter_recurrent-neural-networks/index.html','llm-train'],
    ['注意力与预训练','对照 Q/K/V、位置、编码器/解码器与预训练模式','https://d2l.ai/chapter_attention-mechanisms-and-transformers/index.html','llm-cache'],
    ['从手算到张量工程','优化算法、多 GPU、异步与硬件开销','https://d2l.ai/chapter_computational-performance/index.html','llm-budget']
  ];
  if (typeof module !== 'undefined' && module.exports) module.exports = C;
})(globalThis);
