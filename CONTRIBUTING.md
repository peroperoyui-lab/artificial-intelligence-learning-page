# 开发与贡献指南

欢迎补充课程、改进交互、修正公式和提出新的实验。本项目面向中文学习者，优先让人通过真实计算理解机制，并保留公式、来源和可复现的操作过程。

本文约定如何修改和提交；[目录与架构](docs/ARCHITECTURE.md)说明代码放在哪里；[教学与视觉设计](docs/DESIGN.md)解释设计取舍；[验收记录](docs/VALIDATION.md)保存历史验证范围。以当前代码和对应提交的测试结果为准。

## 1. 从哪里开始

小修正直接提交 PR，无需先开 Issue。涉及框架迁移、存储格式、模型协议或较大的课程重排，先在 Issue 或草稿 PR 说明目标、兼容影响和迁移方案，避免并行工作互相覆盖。

从最新 `main` 建分支，建议使用 `feat/<topic>`、`fix/<topic>`、`docs/<topic>` 或 `refactor/<topic>`。一个 PR 尽量解决一个完整问题；把必要的源码、测试、说明一起提交，避免夹带全仓格式化或无关重构。可以通过 GitHub 网页、桌面客户端或命令行完成，不限定工具或模型。

## 2. 本地运行与检查

运行现有页面不需要安装依赖：直接打开根目录 `index.html`，或使用 Node.js 20+（当前 CI 使用 22）启动静态服务：

```sh
npm start
```

访问 `http://127.0.0.1:4173`，按启动终端里的 Ctrl+C 停止。这个命令不自动打开浏览器，也不安装后台服务；修改源码后刷新即可。

基本回归和构建：

```sh
npm test
npm run build
```

`npm test` 运行数值、状态和构建回归；`npm run build` 生成多文件网站及 `dist/visible-ai-offline.html`。目前没有 `lint` 命令，不要把未配置的检查写成已执行。

浏览器验收需要 Python 3.10+ 和开发工具：

```sh
python -m pip install -r requirements-dev.txt
python -m playwright install chromium
npm run test:e2e
```

浏览器测试自行构建并启动、关闭临时本地服务，不需要提前运行 `npm start`。Linux 环境缺少浏览器系统库时使用 `python -m playwright install --with-deps chromium`。当前验收覆盖 Chromium，不代表所有浏览器已验证。

默认测试模式使用真实 HTTP、file URL 和原生 localStorage。只有受限环境无法导航时，才显式使用 `AI_TEST_IN_MEMORY=1`：此模式会使用测试存储替身，并跳过原生加载与持久化检查。报告必须写明模式和跳过项；返回退出码 0 不代表跳过项已经通过。

## 3. 按改动选择验证范围

| 改动 | 提交前需要提供的证据 |
|---|---|
| 仅 Markdown 或 PR 模板 | 检查相对链接、目录和命令与源码一致；确认 diff 不包含运行代码 |
| 原理、公式、示例数值、课程组织 | 原始来源与适用条件；`npm test`；涉及的章节、引用、搜索和进度显示检查 |
| 数值引擎、优化器、模型导入 | 独立期望值或梯度校验；异常与边界测试；`npm test` 和相关浏览器验收 |
| 页面、样式、控件、课程新增 | `npm test`、`npm run build`、默认浏览器验收；相关桌面/窄屏截图与键盘操作检查 |
| 构建、脚本加载、部署路径、工作流 | 上述基本检查；重点核验多文件与单文件 file 打开、HTTP、项目子路径，以及实际 CI 结果 |

纯文档 PR 不要求重复制作截图或在本地安装浏览器；现有 PR 工作流仍会自动执行全套验收。对代码 PR，缺少本地浏览器环境时应如实说明，由成功的默认 CI 补验后再合并。

修复问题优先增加能复现问题的回归测试。预期结果应来自独立手算、已说明的公式或数值差分，避免用待测函数本身生成“正确答案”。异步训练测试优先等待状态或目标轮次，不依赖恰好等待某个时长。

测试数量会随功能增长，文档中的历史数量不构成永久上限。不要删除有效断言来让 CI 变绿；课程结构确实改变时，同步更新旧的固定数量及相关测试意图。

## 4. 工程边界

- **保留基础课程的离线入口。** 当前生产运行零第三方依赖，使用普通脚本、相对资源和哈希路由。新依赖、框架或真实大模型可以提出，但应交代必要性、许可、下载量和降级行为，避免把外网、账户或密钥变成既有课程的前置条件。
- **复用已有职责。** 通用数值算法优先放 `engine.js`，课程文字放 `content.js`，控件与绘图复用 `ui.js`。新章节按现有挂载约定接入；不必为一个小实验搭建新的框架。
- **显式管理生命周期。** 重复播放优先用 `U.Loop`，离开章节时 `dispose()`；切后台停止，不自动恢复。新增全局监听器、Observer、Worker、异步读取或下载任务也要清理或防止过期回调修改新页面。
- **源码与产物分开。** 修改 `src/`、`assets/`、`index.html` 或构建脚本，不手改 `dist/`。新 JS/CSS 的引入要检查构建器能否内嵌；CSS 的 `url(...)` 等额外资源目前不会自动打包进单文件。

新增代码使用清楚的命名、合理分段和必要注释，说明公式约定、数组形状或非显然的取舍。优先沿用附近的语法和样式约定，不为了格式统一重排无关代码；当前源码中的紧凑写法不要求新代码也压成一行。新样式尽量限定在对应实验容器，避免无意覆盖全站控件。

## 5. 数值与教学质量

每个新实验需要说清：学习目标、输入是什么、计算做了什么、输出如何解读，以及一个可以验证的操作任务。正文用中文，重要术语附英文；数学细节可展开，演示使用的简化条件写在相关位置，不用大段泛化声明替代解释。

数值随参数实际重算；预设序列或人工教学数据要明确标注。训练、推断、模型输出、人工 logits 与随机采样分别说明，不能混为一谈。来源优先教材、官方文档和原始论文，在 `C.sources` 登记并由章节 `refs` 引用；内容用自己的语言组织。

修改训练逻辑时同步检查损失与梯度的常数、求和/平均口径、标签语义、张量维度、随机种子、参数更新及图例。目前训练台的基线为：训练/验证分离、BCE 图不含 L2、L2 不惩罚偏置、Adam 的 L2 加入梯度而非 AdamW。要改变这些约定，可以在同一 PR 中一起更新实现、说明、模型兼容策略与测试。

对极端数值使用稳定计算或明确拒绝并说明原因；指标未定义时保留“未定义”的含义，不悄悄显示为 0。训练发散、导入失败或配置无效时保留清楚的恢复入口，不让错误状态伪装成成功结果。

## 6. 交互、数据与兼容性

保持“实验 → 原理 → 推导 → 自测与笔记”的阅读路径。参数标签、单位、取值范围和图例要对应实际计算；保留暂停、重置和状态反馈。使用有意义的动效，尊重减少动画设置，类别/曲线尽量同时用形状或线型区分。

UI 变更检查 320px、390px 和桌面布局，以及键盘焦点、表单标签、搜索、专注模式。Canvas 交互应保留文本指标和可键盘操作的替代入口；截图、ARIA 属性或测试通过都不自动等于完整无障碍认证。

章节 ID 同时被路由、前置关系、词典和学习记录使用。默认保持已有 ID；改名、删除或迁移必须说明旧链接与旧数据如何处理。不得在普通功能 PR 中默默清空用户笔记、改变存储键或破坏既有模型导入。

模型文件是数据，不执行其中的代码。导入按“验证 → 完整构建候选状态 → 整体替换”处理，失败保留当前模型。修改 `visible-ai-model-v1`、`engineVersion` 或学习记录结构时，说明旧版本的兼容、迁移或明确拒绝策略，并加入相应测试。

用户笔记、导入文件和存储内容视为不可信文本，使用 `textContent` 或 `U.escape`，不要直接拼入 HTML。仓库中的静态 `deep.html` 是受代码审查的课程内容，不是开放给用户执行 HTML 的接口。基础页面不新增隐式遥测、远程调用或秘密凭据；按需在线扩展要明确告知数据流向。

## 7. 提交与合并

PR 使用仓库模板，说明动机、范围、验证结果和兼容影响。界面改动附截图；数学改动附依据或对照值；不适用的检查写明原因。AI 辅助提交与人工提交使用同一验收标准，不要求提供私有提示词或推理过程。

建议提交消息写成 `feat: ...`、`fix: ...`、`docs: ...`、`test: ...` 或 `refactor: ...`，中文或英文均可。合并前检查目标提交的 diff 与 CI；通常使用 squash，方便按一个完整改动回退。不强推共享 `main`，不覆盖别人的提交。

当前 `.github/workflows/ci.yml` 对所有 PR 运行验收。`pages.yml` 在主分支推送时独立构建和发布，并不等待另一工作流的浏览器验收；所以应在合并前完成 PR 验证。本文不代表已经配置 GitHub 分支保护或强制合并门禁。

新增第三方代码、素材或模型时更新 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)，保留所需版权与许可说明。不要把密钥、私人数据、日志、截图中的敏感信息、`dist/`、测试输出或临时 ZIP 混入源码提交；调试证据可放 PR 附件或 CI artifacts。
